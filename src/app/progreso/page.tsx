import { PagePlaceholder } from "@/components/ui/page-placeholder";

export const metadata = { title: "progreso" };

export default function ProgressPage() {
  return (
    <PagePlaceholder
      description="verás avance por área, precisión por tema y una lista manejable de repasos pendientes."
      eyebrow="progreso"
      title="entiende cómo vas."
    />
  );
}
