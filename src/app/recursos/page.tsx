import { PagePlaceholder } from "@/components/ui/page-placeholder";

export const metadata = { title: "recursos" };

export default function ResourcesPage() {
  return (
    <PagePlaceholder
      description="Este espacio reunirá materiales de consulta rápida sin competir con tu ruta principal."
      eyebrow="Recursos"
      title="Consulta solo lo necesario."
    />
  );
}
