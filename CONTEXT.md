# CONTEXT.md

snapshot for resuming work on ceneva — updated 2026-09-25 after the global content audit (item 41), the editorial-grid redesign (item 42) and its interaction/performance correction (item 43). the functional mvp is complete: all 34 units across all seven areas are registered (items 1–40 built the curriculum area by area; item 40 completed the 22-unit expansion queue with `lc-6-5-estructura-gramatical-del-ingles`), giving 177 topics, 184 lessons and 920 questions; diagnostics are pensamiento matemático 30, cultura digital 18, conciencia histórica 21, humanidades 20, ciencias naturales 32, lengua y comunicación 31 and ciencias sociales 25; the cross-area coverage simulator is 177 questions. the responsive shell, light/dark palettes, navigation, lesson structure, question controls, feedback and results use the system in `docs/16-sistema-de-diseno.md`. the global audit (item 41) independently re-verified every one of the 184 lessons and 920 questions against `docs/guiaoficial.pdf`, found and fixed three content defects (`pm-sel-004`, `ch-pde-001`, `lc-uds-005`) and one production-code ui defect (`src/features/lesson/lesson-view.tsx` leaked a lesson's raw prerequisite id to learners instead of its title), and its result is **ready** for auth/synchronization work — see `docs/tasks/2026-09-23-report_expansion-auditoria-global-resultados.md` for the full account. item 42 replaces the previous card-heavy visual composition with instrument sans, a twelve-column editorial grid, ruled indices, smaller radii, teal ambient gradients, active navigation and restrained motion; it preserves every product rule and derives the home count from the real 920 registered questions. item 43 locks each inline lesson answer after its first selection, makes completion visible and synchronized at the lesson header and close, replaces the placeholder footer link with corshex/linkedin/ko-fi, and removes the repeated per-topic lesson loading that made server-rendered navigation slow. its contract remains `docs/tasks/2026-09-25-task_redeseno-reticula-editorial.md`. auth and synchronization may now begin. read this file for the "why" and "where things stand," `CLAUDE.md` for the operating rules.

**`## comprobación`/`## cierre` parsing is case-insensitive.** `src/features/lesson/lesson-view.tsx`'s `get_markdown_sections` previously split lesson bodies on the literal lowercase strings `"\n## comprobación\n"`/`"\n## cierre\n"`, which would have silently dropped those sections if the heading were capitalized instead of erroring. the parser now matches both headings case-insensitively while preserving source casing. all 67 registered lessons (the 37 original mvp lessons plus the 30 `pm-1-2` through `pm-1-6` lessons brought into line by `2026-09-23-task_revision-editorial-pm-expandido.md`) now use `## Comprobación`/`## Cierre`.

## What ceneva is

A web app that turns the official CENEVAL "Acuerdo 286" bachillerato-accreditation guide into a guided, measurable study path: diagnostic → route (area → unit → topic → lesson) → practice with explained feedback → simulacros whose errors link back to the exact topic/lesson to review. It's an independent product — not a reskin or continuation of "cenevalprep," and none of that product's content is reused.

The user is a solo, non-professional developer building this as their main vehicle for learning to code (see `docs/13-desarrollo-asistido-por-ia.md`, `docs/14-plan-de-aprendizaje.md`). They want to understand and be able to explain every part of what gets built, not just have it generated. Collaboration model: they assign work as files in `docs/tasks/`, expect clarifying questions before ambiguous or architecture-affecting changes, and want terse responses ("finished with X" + only necessary extra info — no emojis, no unrequested feature suggestions, no overengineering).

## Users and MVP

Target user: preparing for the Acuerdo 286 exam independently, often while working, limited time, uneven level across subjects. They need to know what to study next, not just a global score (`docs/02-usuario-y-problema.md`).

**MVP covers all seven areas** (pensamiento matemático, cultura digital, conciencia histórica, humanidades, ciencias naturales/experimentales/tecnología, lengua y comunicación, ciencias sociales), each kept as simple as possible: guest mode with local progress, short diagnostic, one navigable route per area, practice with explained feedback, a from-scratch simulator, basic progress dashboard. Build order: validate the full template (content + architecture) on pensamiento matemático first, then replicate the same pattern to the other six areas — still within the MVP, not deferred after it (`docs/07-mvp-y-roadmap.md`, `docs/12-plan-de-entrega.md`).

Out of scope for MVP: AI chat/bot, gamification, community, paid plans, predictive recommendations. An FAQ is in scope as static informational content (not a bot). Nothing in the app is or will be paid; a possible separate paid PDF product is a loose future idea, not a current priority.

## Architecture decisions

- **Stack**: Next.js + TypeScript, Tailwind CSS, Supabase (auth + Postgres) added later without changing the base experience. Deploy target: **Vercel only** (GitHub Pages was dropped — static export would've fought with future Supabase auth), on the free tier as long as possible. Package manager: **pnpm**. Test runner/e2e: **Playwright**.
- **Content**: curriculum, lessons, and questions are versioned Markdown/JSON/TS in the repo (not a CMS), with stable ids that never change once published. See `docs/11-modelo-de-contenido-y-datos.md` for the exact schemas.
- **Progress**: starts in localStorage (versioned schema), migrates to Supabase later behind a storage abstraction so feature code doesn't change.
- **app structure**: the scaffold exists. `app` contains routes and screen composition only; `features` contains curriculum, lesson, practice, progress, diagnostic and simulator modules; `content` is pure editorial data; `components` holds generic ui; and `lib` holds storage, validation and utilities. domain rules such as topic mastery status live in pure, testable functions, never inline in a screen. see `docs/10-arquitectura-de-aplicacion.md`.
- **Simulator and question bank**: both built entirely from zero, sourced from the official guide, AI-assisted under the user's supervision. Nothing from cenevalprep or any prior simulator is reused — this was a real contradiction across docs/03/05/08/09 that's now resolved and reflected in those files.
- **Question format** (confirmed from the actual guide, not assumed): every question has **exactly 3 options** — 1 correct + 2 distractors, never 4. The guide's four reactivo shapes (cuestionamiento directo, jerarquización, completamiento, relación de elementos) all still resolve to 3 lettered options; jerarquización/relación just encode a full sequence or mapping inside one option string. This is now in `docs/11`'s `question` type, which also has `common_error?` and `source_reference?` fields.
- **mastery rule**: the product rule reaches `dominado` at ≥80% accuracy over ≥5 questions with all topic lessons complete, and only returns to `en progreso` below 60% later (`docs/06-datos-y-progreso.md`). etapa 1 intentionally used a smaller symmetric-80% contract; etapa 2 task 1 replaced it with the full 80%/60% hysteresis, derived by replaying each topic's stored attempts in order rather than persisting a computed topic status. important: the guide's own "reactivos oficiales" count per topic (usually 1, occasionally 2) is the real exam's *weighting*, not a target for ceneva's practice bank — most topics need several more authored questions than their official count to clear the mastery minimum.

## Where things actually stand (repo state)

the stage-0 application base and content assets exist. what exists:

- **`docs/00` through `docs/15`** (Spanish): the full product/architecture spec. Several were edited on 2026-09-10 to resolve contradictions discovered during review — see `docs/tasks/2026-09-10-report_project-understanding.md` sections 7–11 for the full list of what changed and why.
- **stage 0 application base**: a next.js 16.3.5, typescript 5.9.3 and tailwind css 4.3.3 application now lives in `src/`. it provides the root layout, responsive global navigation, footer and all initial routes from `docs/10`, plus the secondary `/recursos` route. routes intentionally use presentational placeholders only; no feature or editorial logic was placed in `app/`. the code portion of etapa 0 is complete; its vercel-preview acceptance remains pending until the repository is connected to vercel.
- **quality and ci base**: `pnpm` 11.19.0 is the package manager; `pnpm check` runs prettier, eslint, typecheck, vitest and the production build. playwright covers the base navigation and routes in `e2e/`. `.github/workflows/ci.yml` runs that quality chain and chromium e2e on pull requests and pushes to `main`. `pnpm-workspace.yaml` explicitly allows the required `unrs-resolver` build script, so installs are non-interactive and reproducible.
- **etapa 1, task 1 complete**: `/ruta` now leads to the real `pensamiento-matematico` pilot; `/ruta/pensamiento-matematico` shows `pm-1-1-pensamiento-estadistico` and its four topics; and each stable pilot lesson url reads, validates and renders its markdown frontmatter/body. `src/features/curriculum/` resolves the typed curriculum, while `src/features/lesson/` owns the server-side markdown loader. `gray-matter` and `react-markdown` are production dependencies. only the four pilot lesson ids are exposed; invalid and non-pilot ids use next's not-found response. focused loader tests and the route-to-lesson e2e flow cover this boundary.
- **etapa 1, task 2 complete**: `src/features/practice/` resolves every pilot lesson's declared `question_ids` against the existing typed pensamiento matemático bank, validating topic correspondence, exactly three options, a valid answer index and an explanation. `LessonCheck` is the narrow client component that holds answers only with `useState`; its native radio inputs provide keyboard operation, visible focus and immediate authored feedback. it displays the correct answer, explanation and next action after every selection, without localstorage, completion or progress logic. unit tests cover all four five-question sets; e2e covers a real keyboard response and its feedback.
- **etapa 1, task 3 complete**: `learner_progress` now persists as version 1 through a small repository in `src/lib/storage/`; malformed, incompatible or unavailable storage recovers to empty progress. `src/features/progress/` owns the pure topic-status rule, the client hydration hook and the pilot progress ui. each selected answer records a `practice` attempt, lesson completion is explicit, and `/ruta/pensamiento-matematico` plus `/progreso` derive the same statuses from stored attempts and completed lesson ids. no progress state is duplicated into content or a backend. the test suite covers recovery, unavailable storage, the etapa-1 symmetric-80% rule and a full reload that restores a dominated pilot topic.
- **etapa 2, task 1 complete**: `src/features/practice/pilot-topic-practice.ts` resolves a pilot topic id to its curriculum metadata, its one related lesson and its five authored questions, validating topic correspondence, three distinct options, a valid answer index and an explanation; invalid or non-pilot ids resolve to `undefined` and the route returns not found. `/practica/[topic-id]` now renders the real one-question-at-a-time session (`TopicPracticeCheck` in `features/practice`, wrapped by `TopicPracticeSession` in `features/progress` for persistence): a learner may change their selection before an explicit "confirmar respuesta" submit, which records exactly one `practice` attempt through the existing repository and reveals authored feedback; after five submissions a result view shows the score, each explained outcome and a link back to the lesson. `calculate_topic_progress` in `src/features/progress/pilot-progress.ts` now replays a topic's stored attempts in order to implement the full docs/06 rule: `dominado` is reached at ≥80% accuracy over ≥5 attempts with all topic lessons complete, and is only lost once accuracy drops below 60% — a 60–79% dip after mastery no longer demotes. `/ruta/pensamiento-matematico` and `/progreso` derive from the same function, so they stay consistent by construction. tests cover topic resolution, all four mastery boundaries (fresh sub-80%, exact 80%, retained dip, lost-below-60%) and e2e coverage for a full session, its lesson link, an invalid topic id and reload persistence.
- **etapa 2, task 2 complete**: `/practica` now renders the real five-item review queue instead of a placeholder. `src/features/practice/pilot-review-candidates.ts` resolves all twenty pilot questions with their topic and lesson (pure content, no progress dependency, built on task 1's topic resolver). `src/features/progress/pilot-review.ts`'s `select_pending_review_items` is a pure function that takes those candidates plus the live attempts array: it keeps only each question's latest attempt, filters to incorrect ones, orders by most recent pending attempt (stable tiebreak on stored order), and caps the result at five — attempts for questions outside the pilot are ignored rather than surfaced. `PilotReviewQueue` (`features/progress/pilot-review-queue.tsx`) captures that queue once per page visit after hydration, lets a learner answer each item inline (selecting an option submits immediately, recording one attempt with mode `review` through `use-pilot-progress`'s new `record_review_answer`), shows the authored explanation, and links to the related lesson; an explicit empty state covers no-pending-items, malformed and unavailable storage. Corrected items leave the queue on the next full visit, since a fresh candidate+attempts computation runs on remount. Tests cover deduplication, latest-attempt precedence, ordering/tiebreak, the five-item cap and foreign-attempt filtering (unit), plus a full incorrect-then-corrected review cycle, reload removal and malformed-storage safety (e2e).
- **etapa 2, task 3 complete — etapa 2 is now fully done**: `src/features/progress/pilot-summary.ts` adds two pure functions composed from the already-computed `topic_progress[]` (task 1) and a pending-review count (task 2), never recalculating either: `calculate_pilot_summary` aggregates dominated topics, completed/total lessons and overall accuracy across the pilot; `get_pilot_next_action` picks the first accessible (non-`bloqueado`) non-`dominado` topic and recommends its first incomplete lesson, or practice for that topic once its lesson(s) are done, or a `completed` state once every topic is dominated. `use-pilot-dashboard.ts` is the shared hydration-safe hook wiring `usePilotProgress` to both functions plus the review selector; `PilotHomeGuidance` (home) and `PilotProgressSummary` (`/progreso`) both consume it and share one `PilotNextActionCta` presentational component so the two pages never diverge. the home hero at `src/app/page.tsx` is unchanged except for this new section below its existing CTAs; `/progreso` gained the compact stat block above the existing per-topic list. unit tests cover empty progress, a practice recommendation once a lesson is done, skipping dominated/blocked topics, the fully-dominated case and summary aggregation; e2e covers the empty-progress recommendation (via keyboard activation), the post-completion recommendation on both pages surviving reload, and a mobile-viewport visibility check.
- **etapa 3, task 1 complete**: `/diagnostico` runs a four-question pensamiento matemático pilot diagnostic. `src/features/diagnostic/pilot-diagnostic.ts`'s `get_pilot_diagnostic` resolves the four documented existing questions (`pm-tv-001`, `pm-tm-001`, `pm-mtc-001`, `pm-md-003`, one per pilot topic, in curriculum order) by reusing `get_pilot_topic_practice`'s topic/lesson resolution and validation (topic correspondence, three distinct options, valid answer index, explanation) rather than duplicating it. `PilotDiagnosticCheck` (`features/diagnostic`) is a one-question-at-a-time client component — a learner may change their selection before an explicit "siguiente pregunta"/"ver resultado" submit, and unlike topic practice it does not reveal correctness or explanation per question; `PilotDiagnosticSession` (`features/progress`, wired the same way as `TopicPracticeSession`) records each submission as exactly one `diagnostic` attempt through the existing repository via a new `record_diagnostic_answer` in `use-pilot-progress.ts`. After the fourth submission, the result view shows the score, each topic's explained outcome, and one recommendation computed by `get_pilot_diagnostic_recommendation` (`features/diagnostic/pilot-diagnostic-recommendation.ts`): the first topic answered incorrectly in curriculum order, or — if every answer is correct — the first topic whose `topic_progress.status !== "bloqueado"`, reusing `create_pilot_topic_progress_definitions`/`calculate_topic_progress` from `features/progress` rather than a new accessibility check. Diagnostic attempts are ordinary attempts: they feed the same 80%/60% mastery calculation as any other mode, by design (see `docs/tasks/2026-09-15-report_product-decisions.md`). No session, result or recommendation schema was added; reloading `/diagnostico` resets the visual session to question one while prior diagnostic attempts remain in progress history. Unit tests cover the resolver's fixed selection and validation, and the recommendation function's incorrect-topic order, all-correct fallback and no-accessible-topic fallback; e2e covers keyboard operation with no reveal until the end, a full correct diagnostic and its lesson link, a mixed-result diagnostic's recommendation, and diagnostic-attempt persistence across reload of both `/diagnostico` and `/ruta/pensamiento-matematico`.
- **etapa 3, task 2 complete — etapa 3 is now fully done**: `/simulacro` runs the twenty existing pilot simulator-capable questions. `src/features/simulator/pilot-simulator.ts`'s `get_pilot_simulator` groups them into the four pilot topics (five questions each, in curriculum and lesson-question order) by reusing `get_pilot_topic_practice` per topic, validating the `simulator` use case the same way the diagnostic resolver validates `diagnostic`; `flatten_pilot_simulator_groups` is a pure helper producing the flat 20-item sequence the session needs. `PilotSimulatorCheck` (`features/simulator`) is a one-question-at-a-time client component structurally like `PilotDiagnosticCheck` but kept as its own component (see `docs/tasks/2026-09-15-report_product-decisions.md`): no correctness or explanation is revealed before the twentieth submission. `PilotSimulatorSession` (`features/progress`) records each submission as one `simulator` attempt via a new `record_simulator_answer` in `use-pilot-progress.ts`. The final report is computed by the pure `calculate_pilot_simulator_report` (`features/simulator/pilot-simulator-report.ts`): total score, then one group per topic with its correct/total count, accuracy, and — unlike the diagnostic's full per-response listing — only the incorrect responses, each with the learner's answer, the correct answer, the authored explanation and a link to that topic's related lesson. No simulator-session or result schema was added; reloading `/simulacro` at any point starts a fresh visual session while prior `simulator` attempts remain ordinary progress history (feeding mastery/accuracy like any attempt, same as diagnostic attempts). Unit tests cover the resolver's grouping/order/validation and the report function's score calculation, per-topic grouping and error-to-lesson data; e2e covers keyboard operation with no reveal until the report, a full run with one deliberate error verifying the grouped report and its lesson link, persistence of simulator attempts across reload of both `/simulacro` and `/ruta/pensamiento-matematico`, and a mobile-viewport interaction check.
- **etapa 3 is complete.** the next planned work is etapa 5 (replicating the validated pattern to the remaining six areas) plus continued editorial work per the "immediate next steps" below.
- **etapa 5, task 1 complete**: `cd-2-1-ciudadania-digital` is the selected second vertical because its five lesson files already existed with five stable reserved question ids each, while its typed bank and application support did not. `content/questions/cultura-digital.ts` now has the 25 authored questions (five per topic), traceable to the guide, calibrated against its cultura digital sample material. `content/questions/types.ts` holds the `question`/`question_use_case` types shared by both banks now (pensamiento matemático's records/ids untouched — the migration is import-only). `src/lib/validation/question-bank.ts` has the pure structural/coverage/traceability checks, validated by `src/lib/validation/question-bank.test.ts`.
- **etapa 5, task 2 complete**: the pilot-only content boundary is generalized into an explicit two-unit registry. `src/features/curriculum/available-units.ts` lists exactly the two content-ready units (pensamiento matemático `pm-1-1-pensamiento-estadistico`, cultura digital `cd-2-1-ciudadania-digital`) with their typed area and question bank. `src/features/curriculum/available-curriculum.ts`'s `get_available_unit`/`get_unit_questions` resolve an `(area_id, unit_id)` pair against that registry, failing safely (`undefined`) for any unknown, incomplete or cross-area pairing. `src/features/lesson/unit-lessons.ts` generalizes the markdown loader to `content/lessons/<area_id>/<unit_id>/<lesson_id>.md` with the same frontmatter validation as before (area/unit/topic membership, title-led body, source, prerequisites), now parametrized instead of hardcoded to pensamiento matemático. `src/features/practice/unit-topic-content.ts` resolves a topic to its lesson(s) and questions, attributing every question to the exact lesson that reserved its id via `lesson.question_ids` — `resolve_topic_questions` is a pure function supporting a topic with one or more lessons, tested with an in-memory multi-lesson fixture (no real content is multi-lesson yet). The existing pilot-facing functions (`get_pilot_curriculum`, `get_pilot_lessons`, `get_pilot_topic_practice`, `get_pilot_lesson_questions`) keep their exact prior names, signatures and behavior — they are now thin wrappers bound to the pensamiento matemático ids, delegating to the generic boundary. Nothing else needed to change: `pilot-review-candidates.ts`, `pilot-diagnostic.ts`, `pilot-simulator*.ts`, `pilot-progress.ts` and every route/component call these same wrapper functions and are untouched. No route, screen or navigation entry for culture digital exists yet; `pnpm build`'s route list is unchanged. Tests cover the registry's exact two entries and stable order, resolution/fail-safe behavior for both real units and invalid pairings, the generic lesson loader for all 4 pensamiento matemático + all 5 cultura digital lessons, and topic/question resolution for both real units plus the multi-lesson fixture.
- **etapa 5, task 3 complete — etapa 5 is now fully done**: culture digital is a second navigable vertical, composed entirely from task 2's generic boundary plus the existing presentational/progress patterns — no separate culture-digital feature copy was created. `/ruta` now renders one card per `available_units` entry instead of a single hardcoded pensamiento matemático card, so it lists exactly the two content-ready areas. `/ruta/[area]` resolves the requested area through the new `get_available_unit_for_area` (`available-curriculum.ts`) and renders the generic `UnitRoute` (`features/curriculum/unit-route.tsx`, replacing the old pensamiento matemático-only `PilotUnit`), so `/ruta/cultura-digital` shows `cd-2-1-ciudadania-digital`'s five topics with the same status/prerequisite/accuracy derivation as pensamiento matemático — including `medidas de seguridad digital` correctly rendering `bloqueado` until its prerequisite lesson is complete. `/leccion/[lesson-id]` now resolves through the new `get_available_lesson` (`unit-lessons.ts`, tries every registered unit by stable lesson id) into the renamed, generalized `LessonView` (`features/lesson/lesson-view.tsx`, replacing `PilotLesson`), which derives its "volver a…" link and its comprobación questions from the lesson's own `area_id`/`unit_id` via the new `get_lesson_questions_for_unit` (`unit-topic-content.ts`) instead of a hardcoded pensamiento matemático bank. `/practica/[topic-id]` resolves through the new `get_available_topic_practice` (built on a new `get_unit_topic_practice`, both in `unit-topic-content.ts`) into the renamed `TopicPracticeView` (replacing `PilotTopicPractice`). `pilot-topic-practice.ts`'s `get_pilot_topic_practice` — still the only entry point diagnostic, simulator and review use, and deliberately unchanged in name/signature/behavior — is now a one-line wrapper over `get_unit_topic_practice`, so none of those pensamiento matemático-only callers needed to change. `PilotTopicProgressList` gained a required `aria_label` prop so its accessible name is no longer hardcoded to "temas de pensamiento estadístico"; both its callers (the new `UnitRoute` and the untouched `/progreso` overview) pass `` `temas de ${unit.title}` ``, so pensamiento matemático's rendered output and existing progress/mastery derivation are unchanged. Once the shared boundary covered their job, the now-dead pensamiento matemático-only duplicates were deleted rather than left as backwards-compatibility shims: `pilot-unit.tsx`, `pilot-lesson.tsx`, `pilot-lesson-questions.ts` (+ its test), `pilot-topic-practice-view.tsx`. `/diagnostico`, `/simulacro`, root `/practica` review, home guidance and `/progreso`'s pensamiento matemático summary are untouched, as are all ids, the local-progress schema and the 80%/60% mastery rule — culture digital reaches `dominado` through the exact same replay-based `calculate_topic_progress`, proven in this task by completing `cd-identidad-digital-01` and answering its topic practice. Unit tests cover the three new cross-unit resolvers (`get_available_unit_for_area`, `get_available_lesson`, `get_available_topic_practice`) against both real units, invalid ids and unregistered units; `e2e/navigation.spec.ts` gained three tests — keyboard-driven comprobación feedback on a culture digital lesson, the full `/ruta` → `/ruta/cultura-digital` → lesson → completion → direct topic practice → explained result → related lesson → reload flow (asserting the topic reaches and keeps `dominado`, and that pensamiento matemático's `tipos de variables` stays `disponible`/untouched in the same browser context as a cross-area isolation check), and one mobile-viewport (375×667) practice interaction — plus the three new urls added to the base-routes smoke test. format check, lint, typecheck, the full unit suite (77 tests across 18 files), the production build (route list unchanged: `/leccion/[lesson-id]`, `/practica/[topic-id]` and `/ruta/[area]` remain the only dynamic segments) and the full 25-test chromium e2e suite all pass.
- **etapa 5, conciencia histórica `ch-3-1`, task 1 complete**: the six lesson files for `ch-3-1-mexico-antiguo-y-virreinal-en-contextos-globales` now exist at `content/lessons/conciencia-historica/ch-3-1-mexico-antiguo-y-virreinal-en-contextos-globales/` — `ch-conquista-de-pueblos-originarios-01`, `ch-resistencias-de-pueblos-originarios-01`, `ch-impacto-cultural-de-resistencias-originarias-02`, `ch-grupos-sociales-de-la-nueva-espana-01`, `ch-origen-del-patrimonio-historico-01` and `ch-preservacion-del-patrimonio-historico-01`, each with valid frontmatter, its exact prerequisite chain and five reserved question ids traceable to guide page 13. the `ch-3-1-2` topic keeps its two lessons genuinely distinct: the first teaches the guerra de castas and the resistencia yaqui as historical events, the second teaches only their present-day cultural manifestations (e.g. the danza del venado, felipe carrillo puerto). `ch-origen-del-patrimonio-historico-01` is calibrated against the guide's own conciencia histórica sample question (`docs/guiaoficial.pdf`, page 31, palacio nacional's origin), the only sample in that block applicable to this unit; its `## comprobación` states that calibration boundary explicitly (origin vs. site antecedent vs. current use) for task 2 to pick up. no question records, `available_units` registration, routes or application code were touched — task 2 (thirty-question bank) and task 3 (registration and visible integration) remain pending. format check, lint, typecheck and the full unit suite (77 tests, unchanged) pass, since only new markdown content was added.
- **etapa 5, conciencia histórica `ch-3-1`, task 2 complete**: `content/questions/conciencia-historica.ts` now has 30 typed records — five per lesson, with `ch-3-1-2`'s two lessons (`ch-resistencias-de-pueblos-originarios-01`, `ch-impacto-cultural-de-resistencias-originarias-02`) each keeping its own five ids and its distinct objective (historical event vs. current cultural manifestation) despite sharing one topic id. Every record has three distinct options, a valid answer index, an explanation, a concrete `common_error`, a `source_reference` to guide page 13 and its topic's code (`3.1.1`–`3.1.5`), a difficulty and use cases; direct, completion and relation shapes are used across the unit, and one ordering question (the yaqui despojo → resistencia → represión → deportación causal chain) is used where a real historical sequence exists, per `ch-rpo-005`. `ch-oph-001` reproduces the guide's page-31 origin-vs-antecedent-vs-current-use distinction applied to the palacio nacional, without copying its wording. No shared type, existing bank, source map, lesson frontmatter, `available_units`, route or application behavior was touched. `src/lib/validation/question-bank.test.ts` gained a second describe block deriving all six lesson ids' `topic-id`/`question-ids` from frontmatter (no duplicated assumptions) to prove: exactly 30 ids matching the reserved table; each lesson's own five ids resolve to records whose `topic_id` matches that lesson; topic `ch-3-1-2` has exactly 10 questions splitting into the two lessons' reserved sets; no structural errors; no id collision across all three banks; and source-code traceability to page 13 and the right `3.1.x` code. format check, lint, typecheck, the full unit suite (84 tests across 18 files, up from 77) and the production build (route list unchanged) all pass. task 3 (registration and visible multi-lesson integration) remains pending — the third vertical is not yet complete.
- **etapa 5, conciencia histórica `ch-3-1`, task 3 complete — conciencia histórica is now the third navigable mvp vertical, and etapa 5's `ch-3-1` batch is fully done**: `src/features/curriculum/available-units.ts` registers exactly `conciencia-historica`/`ch-3-1-mexico-antiguo-y-virreinal-en-contextos-globales` as the third entry, so `/ruta` lists the three ready areas and `/ruta/conciencia-historica` renders its five topics and six lessons through the same generic `UnitRoute`/`LessonView` boundary cultura digital already proved — no area-specific feature copy was created. `ch-3-1-2-movimientos-de-resistencia-de-pueblos-originarios` is the project's first real multi-lesson direct-practice case (`ch-resistencias-de-pueblos-originarios-01` + `ch-impacto-cultural-de-resistencias-originarias-02`, five questions each). `src/features/practice/unit-topic-content.ts`'s existing `get_topic_content`/`resolve_topic_questions` (built in task 2, previously unused by any route) already attributed every question to its exact declaring lesson; this task added `get_available_topic_content`, a cross-unit wrapper over `get_topic_content` parallel to the existing `get_available_lesson`, and pointed `/practica/[topic-id]` at it instead of the old single-lesson `get_available_topic_practice` (deleted as dead code once nothing called it — the established practice from etapa 5 task 3 for culture digital). `TopicPracticeView`, the new `TopicPracticeSession` and `TopicPracticeCheck` now take `content: topic_content` (plural `lessons` and `topic_question[]`, each item pairing a `question` with its declaring `lesson`) instead of a single `lesson`/`question[]` pair: the header renders the question count dynamically and one "repasar" link per unique lesson; the result view shows a "volver a" link per unique lesson (still exactly one for every existing single-lesson topic, so cultura digital and pensamiento matemático's direct-practice UI is byte-for-byte unchanged) and, only when a topic has more than one lesson, an extra per-question "repasar {lesson}" link scoped to that question's own declaring lesson. `get_unit_topic_practice`/`pilot_topic_practice`/`get_pilot_topic_practice` were not touched in name, signature or behavior — diagnostic, simulator and review, the only callers of that single-lesson wrapper, are unaffected and remain pensamiento matemático-only. The topic's prerequisite chain (conquista → resistencias → impacto cultural; origen → preservación) and its `dominado` derivation needed no code change: `create_pilot_topic_progress_definitions` and `calculate_topic_progress` already generalized to multi-lesson topics in etapa 5 task 2 (a topic's `prerequisite_lesson_ids` is the union of its lessons' own `prerequisites`, so `3.1.2` unblocks once both `conquista` and `resistencias` are complete, and reaches `dominado` only once both of its own lessons are complete and its replayed attempt history clears the existing ≥5-attempts/80% entry rule). One real bug was found and fixed by the browser (not by lint/typecheck/unit tests) while writing e2e coverage: the first draft of `TopicPracticeCheck` dereferenced `questions[current_index].question` unconditionally on every render, which threw once `current_index` advanced past the last question (the brief instant before the finished-result branch renders); the fix defers that read until after the `is_finished` early return, matching the original single-lesson component's structure. Unit tests cover the third registry entry and its question count, `get_available_unit`/`get_available_unit_for_area`/`get_unit_questions` for conciencia histórica (including that `ch-3-2`/`ch-3-3` stay unregistered), all six lessons via `get_unit_lessons`/`get_available_lesson` (titles, the split topic's shared `topic_id`, its two-link prerequisite chain), and `get_topic_content`/`get_available_topic_content` for the real ten-question `3.1.2` mapping (exact question-to-lesson attribution, dynamic lesson/question counts). `e2e/navigation.spec.ts` gained five tests: route rendering of the three routes plus the blocked/available split-topic state; the full conquista → resistencias → impacto-cultural lesson chain (asserting real prerequisite links) → the ten-question `/practica/ch-3-1-2-...` flow (dynamic count, both header "repasar" links, all ten per-question result links pointing to the correct one of two lessons, both "volver a" links) → `dominado` → reload persistence → cross-area isolation (pensamiento matemático and cultura digital untouched); one keyboard-operation test on the split topic; and one mobile-viewport (375×667) interaction on a single-lesson topic — plus the three new urls added to the base-routes smoke test. format check, lint, typecheck, the full unit suite (90 tests across 18 files, up from 84), the production build (route list unchanged: `/leccion/[lesson-id]`, `/practica/[topic-id]` and `/ruta/[area]` remain the only dynamic segments) and the full 29-test chromium e2e suite all pass. `/diagnostico`, `/simulacro`, root `/practica` review, home guidance, `/progreso`'s pensamiento matemático summary, mastery thresholds, the local-progress schema, accounts and supabase were not touched.
- **etapa 5, humanidades `hu-4-1`, tasks 1–2 complete**: the four lesson files for `hu-4-1-fundamentos-del-pensamiento-filosofico` exist at `content/lessons/humanidades/hu-4-1-fundamentos-del-pensamiento-filosofico/` — `hu-filosofia-mito-y-ciencia-01` (no prerequisites, the unit's foundation lesson), `hu-pensamiento-critico-01`, `hu-pensamiento-existencialista-01` and `hu-doxa-y-episteme-01` (each requiring `hu-filosofia-mito-y-ciencia-01`), each with five reserved question ids traceable to guide page 14. `content/questions/humanidades.ts` has the 20 typed records (five per lesson), every question tracing to its topic's `4.1.x` code and page 14, with at least one relation question per lesson. `src/lib/validation/question-bank.test.ts` gained a third describe block deriving all four lesson ids' `topic-id`/`question-ids` from frontmatter to prove: exactly 20 ids matching the reserved table, no structural errors, no id collision across all four banks, and source traceability. no `available_units` registration, routes or application code were touched by these two tasks.
- **etapa 5, humanidades `hu-4-1`, task 3 complete — humanidades is now the fourth navigable mvp vertical, and etapa 5's `hu-4-1` batch is fully done**: `src/features/curriculum/available-units.ts` registers exactly `humanidades`/`hu-4-1-fundamentos-del-pensamiento-filosofico` as the fourth entry — the only production-code change this task needed, since `hu-4-1`'s four one-lesson topics needed no new abstraction and could compose the exact generic boundary cultura digital and conciencia histórica already proved (`UnitRoute`, `LessonView`, `TopicPracticeView` via `get_available_topic_content`, `create_pilot_topic_progress_definitions`/`calculate_topic_progress`). `/ruta` now lists the four ready areas and `/ruta/humanidades` renders its four topics with correct statuses: `hu-4-1-1-filosofia-mito-y-ciencia` is `disponible` first, while the other three topics stay `bloqueado` until `hu-filosofia-mito-y-ciencia-01` is completed, then unlock (their real, existing `prerequisites: [hu-filosofia-mito-y-ciencia-01]` frontmatter — the prerequisite graph needed no new code, only registration). direct practice works for all four topics through the existing single-lesson `get_unit_topic_practice`/`get_available_topic_content` path, recording ordinary `practice` attempts; a topic reaches `dominado` through the same replay-based `calculate_topic_progress` once its lesson is complete and its history clears the existing ≥5-attempts/80% rule, proven in this task by dominating `hu-4-1-1-filosofia-mito-y-ciencia`. `/diagnostico`, `/simulacro`, root `/practica` review, home guidance, `/progreso`'s pensamiento matemático summary, mastery thresholds, the local-progress schema, accounts and supabase were not touched, and no humanidades-specific route, session or component was created. Unit tests cover the fourth registry entry and its question count, `get_available_unit`/`get_available_unit_for_area`/`get_unit_questions` for humanidades (plus that `hu-4-2` stays unregistered), all four lessons via `get_unit_lessons`/`get_available_lesson` (the foundation lesson's empty `prerequisites` and the other three lessons' real dependency on it), and `get_topic_content`/`get_available_topic_content` for two real single-lesson topics. `e2e/navigation.spec.ts` gained three tests: route rendering showing the foundation topic `disponible` and the three dependent topics `bloqueado`; the full flow — complete the foundation lesson, confirm a dependent lesson's prerequisite link and its topic's unlock to `disponible`, keyboard-driven five-question topic practice on the foundation topic (arrow-key selection plus mouse-driven remaining answers, no reveal before each explicit submit), the results view's "volver a" lesson link, `dominado` status surviving reload, and cross-area isolation (pensamiento matemático, cultura digital and conciencia histórica all unaffected) — plus one mobile-viewport (375×667) practice interaction — plus the four new urls (`/ruta/humanidades`, one lesson, one topic practice) added to the base-routes smoke test. format check, lint, typecheck, the full unit suite (102 tests across 18 files, up from 90), the production build (route list unchanged: `/leccion/[lesson-id]`, `/practica/[topic-id]` and `/ruta/[area]` remain the only dynamic segments) and the full 32-test chromium e2e suite all pass.
- **etapa 5, ciencias naturales `cn-5-1`, tasks 1–2 complete (done before this session; not previously reflected here)**: `cn-5-1-materia-y-sus-interacciones` is the fifth mvp vertical because its five topics form one complete unit and its only prerequisite (`cn-conservacion-de-la-materia-01` → `cn-tipos-de-enlaces-01`) stays inside that boundary. its three sequential contracts are `docs/tasks/2026-09-16-task_etapa-5-cn-5-1-lecciones.md`, `docs/tasks/2026-09-16-task_etapa-5-cn-5-1-banco.md` and `docs/tasks/2026-09-16-task_etapa-5-cn-5-1-integracion.md`. task 1's five lesson files exist at `content/lessons/ciencias-naturales-experimentales-y-tecnologia/cn-5-1-materia-y-sus-interacciones/`: `cn-tipos-de-enlaces-01`, `cn-estados-de-agregacion-01`, `cn-conservacion-de-la-materia-01`, `cn-conversion-de-temperatura-01` and `cn-ley-de-coulomb-01`, each with its five reserved question ids (`cn-enl-00x`, `cn-eam-00x`, `cn-cm-00x`, `cn-ct-00x`, `cn-lco-00x`) and traceable to guide page 15, codes `5.1.1`–`5.1.5`. `cn-conservacion-de-la-materia-01` correctly requires `cn-tipos-de-enlaces-01`; the other four have no prerequisite. `cn-conversion-de-temperatura-01` and `cn-ley-de-coulomb-01` show formula, substitution, arithmetic and units step by step; `cn-conservacion-de-la-materia-01` verifies atom counts explicitly for real balanced/unbalanced equations. only one ciencias naturales guide sample applies to this unit (page 35, ionic-bond question), cited in `cn-tipos-de-enlaces-01`'s `## comprobación`. task 2 added `content/questions/ciencias-naturales-experimentales-y-tecnologia.ts` with the 25 typed records (five per lesson, ids `cn-enl-001`–`cn-lco-005`), each with three options, explanation, `common_error`, `source_reference` (page 15, codes `5.1.1`–`5.1.5`), difficulty and use cases; direct, completion, relation and ordering shapes are all used, and the calculation lessons' questions show formula/substitution/arithmetic/unit in their explanations. `src/lib/validation/question-bank.test.ts` gained a fourth describe block deriving all five lesson ids' `topic-id`/`question-ids` from frontmatter to prove the 25 ids, per-lesson correspondence and no cross-bank id collision. no `available_units` registration or routes were touched by these two tasks.
- **etapa 5, ciencias naturales `cn-5-1`, task 3 complete — ciencias naturales, experimentales y tecnología is now the fifth navigable mvp vertical, and etapa 5's `cn-5-1` batch is fully done**: `src/features/curriculum/available-units.ts` registers exactly `ciencias-naturales-experimentales-y-tecnologia`/`cn-5-1-materia-y-sus-interacciones` as the fifth entry — the only production-code change this task needed, since `cn-5-1`'s five one-lesson topics needed no new abstraction and composed the exact generic boundary the other four verticals already proved (`UnitRoute`, `LessonView`, `TopicPracticeView` via `get_available_topic_content`, `create_pilot_topic_progress_definitions`/`calculate_topic_progress`). `/ruta` now lists the five ready areas and `/ruta/ciencias-naturales-experimentales-y-tecnologia` renders its five topics with correct statuses: `cn-5-1-1`, `cn-5-1-2`, `cn-5-1-4` and `cn-5-1-5` are `disponible` from the start, while `cn-5-1-3-ley-de-conservacion-de-la-materia` stays `bloqueado` until `cn-tipos-de-enlaces-01` is completed, then unlocks — the real, existing `prerequisites: [cn-tipos-de-enlaces-01]` frontmatter needed no new code, only registration. direct practice works for all five topics through the existing single-lesson `get_unit_topic_practice`/`get_available_topic_content` path, recording ordinary `practice` attempts; a topic reaches `dominado` through the same replay-based `calculate_topic_progress` once its lesson is complete and its history clears the existing ≥ 5-attempts/80% rule, proven in this task by dominating `cn-5-1-1-tipos-de-enlaces`. `/diagnostico`, `/simulacro`, root `/practica` review, home guidance, `/progreso`'s pensamiento matemático summary, mastery thresholds, the local-progress schema, accounts and supabase were not touched, and no ciencias-naturales-specific route, session or component was created. Unit tests cover the fifth registry entry and its question count, `get_available_unit`/`get_available_unit_for_area`/`get_unit_questions` for ciencias naturales (plus that `cn-5-2` stays unregistered), all five lessons via `get_unit_lessons`/`get_available_lesson` (the four no-prerequisite lessons and conservation's real dependency on bond types), and `get_topic_content`/`get_available_topic_content` for two real single-lesson topics. `e2e/navigation.spec.ts` gained three tests: route rendering showing four topics `disponible` and conservation `bloqueado`; the full flow — complete the bond-types lesson, confirm conservation's prerequisite link and its topic's unlock to `disponible`, keyboard-driven five-question topic practice on the bond-types topic (arrow-key selection plus mouse-driven remaining answers, no reveal before each explicit submit), the results view's "volver a" lesson link, `dominado` status surviving reload, and cross-area isolation (pensamiento matemático, cultura digital, conciencia histórica and humanidades all unaffected) — plus one mobile-viewport (375×667) practice interaction on the temperature-conversion topic — plus the three new urls (`/ruta/ciencias-naturales-experimentales-y-tecnologia`, one lesson, one topic practice) added to the base-routes smoke test. format check, lint, typecheck, the full unit suite (115 tests across 18 files, up from 102), the production build (route list unchanged: `/leccion/[lesson-id]`, `/practica/[topic-id]` and `/ruta/[area]` remain the only dynamic segments) and the full 35-test chromium e2e suite all pass.
- **etapa 5, lengua y comunicación `lc-6-1`, tasks 1–3 complete — lengua y comunicación is now the sixth navigable mvp vertical**: `lc-6-1-estrategias-de-comprension-lectora` provides four one-lesson topics and its twenty-question typed bank. `available_units` registers exactly `lengua-y-comunicacion`/`lc-6-1-estrategias-de-comprension-lectora` as the sixth entry, so `/ruta` lists six ready areas and the generic route, lesson, direct-practice and local-progress boundaries expose its four lessons and topics with no new architecture. all four topics have no lesson prerequisites, so they start `disponible`; each still reaches `dominado` only with its own completed lesson plus the existing replayed ≥5-attempt/80% rule. its Spanish guide samples on printed pages 37–38 belong to `6.3`, not `6.1`, and were therefore used only to calibrate the three-option and relation/ordering format. diagnostic, simulator, root review, home guidance and `/progreso`'s pensamiento matemático summary remain pilot-only. tests cover the sixth registry entry, content resolution, direct practice, reload, keyboard, mobile and cross-area isolation.
- **etapa 5, ciencias sociales `cs-7-1`, tasks 1–3 complete — ciencias sociales is now the seventh navigable mvp vertical, etapa 5 is fully done, and the mvp's seven areas are all navigable**: `cs-7-1-organizacion-economica` (nine one-lesson topics, `content/lessons/ciencias-sociales/cs-7-1-organizacion-economica/`) has a real internal prerequisite graph, unlike the five single-batch verticals before it — needs, factors and formal/informal employment start `disponible`; factors unlocks sectors and distribution; distribution unlocks state redistribution; state redistribution unlocks the welfare state; the welfare state unlocks the neoliberal model; sectors unlocks environmental degradation. `content/questions/ciencias-sociales.ts` has the forty-five-question typed bank (five per lesson). `available_units` registers exactly `ciencias-sociales`/`cs-7-1-organizacion-economica` as the seventh entry — the only production-code change task 3 needed, composing the exact generic boundary (`UnitRoute`, `LessonView`, `TopicPracticeView` via `get_available_topic_content`, `create_pilot_topic_progress_definitions`/`calculate_topic_progress`) every prior vertical already proved; the multi-step chain needed no new progress code because a topic's accessibility already derives from its own lesson's `prerequisites` frontmatter, regardless of how many links deep that chain runs. `/ruta` now lists all seven mvp areas; `/ruta/ciencias-sociales` renders the nine topics with the documented graph. direct practice works for all nine topics through the existing single-lesson `get_unit_topic_practice`/`get_available_topic_content` path, recording ordinary `practice` attempts — proven in this task by dominating the factors-of-production topic after completing its lesson and one full five-question run. `cs-7-2-perspectivas-politicas` and `cs-7-3-problemas-sociologicos` (curriculum-only, no lessons or bank) stay unregistered and unreachable, same treatment as every other area's non-pilot units. `/diagnostico`, `/simulacro`, root `/practica` review, home guidance, `/progreso`'s pensamiento matemático summary, mastery thresholds, the local-progress schema, accounts and supabase were not touched, and no ciencias-sociales-specific route, session or component was created. tests cover the seventh registry entry and question count, `get_available_unit`/`get_available_unit_for_area`/`get_unit_questions` for ciencias sociales (plus that `cs-7-2`/`cs-7-3` stay unregistered), all nine lessons via `get_unit_lessons`/`get_available_lesson` (the full prerequisite graph), and `get_topic_content`/`get_available_topic_content` for a real single-lesson topic. `e2e/navigation.spec.ts` gained three tests: route rendering (three topics `disponible`, six `bloqueado`); the full chain-unlock flow — complete factors → sectors and distribution unlock → complete sectors → degradation unlocks → complete distribution → state redistribution unlocks → complete state redistribution → welfare state unlocks → complete welfare state → neoliberalism unlocks → keyboard-driven five-question practice on the factors topic (no reveal before each explicit submit) → its result view's "volver a" lesson link → `dominado` status surviving reload → cross-area isolation (pensamiento matemático, cultura digital and lengua y comunicación all unaffected) — plus one mobile-viewport (375×667) practice interaction on the needs topic — plus the three new urls (`/ruta/ciencias-sociales`, one lesson, one topic practice) added to the base-routes smoke test. format check, lint, typecheck, the full unit suite (141 tests across 18 files, up from 128), the production build (route list unchanged: `/leccion/[lesson-id]`, `/practica/[topic-id]` and `/ruta/[area]` remain the only dynamic segments) and the full 41-test chromium e2e suite all pass.
- **`content/source-maps/<area-id>.md`** (7 files, one per area, all done): the area → unit → topic curriculum tree extracted from `docs/guiaoficial.pdf` pages 11–20, plus a full lesson-breakdown plan per area. Verified line-by-line against the actual PDF on 2026-09-10 — accurate, including edge cases (split-reactivo topics, a genuinely missing `6.2.1` code in lengua y comunicación).
- **`content/curriculum/<area-id>.ts`** (7 files plus `types.ts`): the typed runtime representation of every source-map tree. it contains 34 units, 177 topics and 184 stable lesson references, with official guide code and page metadata at area, unit and topic level. it was compared mechanically with every source map on 2026-09-14, including the page split within `ch-3-2`.
- **`content/lessons/pensamiento-matematico/pm-1-1-pensamiento-estadistico/`** (4 files): the first fully-written unit — tipos de variables, tipos de muestra, medidas de tendencia central, medidas de dispersión. this is the pilot/reference unit for the whole project's lesson-authoring pattern.
- **`content/lessons/pensamiento-matematico/pm-1-2-pensamiento-probabilistico/`** (3 files): the second pensamiento matemático unit — combinaciones y permutaciones, probabilidad simple, probabilidad condicional y teorema de bayes.
- **`content/lessons/pensamiento-matematico/pm-1-3-pensamiento-algebraico/`** (10 files): expressions, factoring, quadratic optimization, linear and quadratic equations, systems, and simple and compound interest.
- **`content/lessons/pensamiento-matematico/pm-1-4-pensamiento-aritmetico/`** (5 files): mcm, mcd, sequences, proportionality, and monetary percentages.
- **`content/lessons/pensamiento-matematico/pm-1-5-pensamiento-geometrico/`** (4 files): areas, triangle similarity, the pythagorean theorem, and coordinate-plane areas.
- **`content/lessons/pensamiento-matematico/pm-1-6-pensamiento-variacional/`** (8 files): inequalities, graphs, limits, derivatives, and optimization. all six pensamiento matemático units are now written: 34 lessons with five stable reserved question ids each.
- **`content/lessons/cultura-digital/cd-2-1-ciudadania-digital/`** (5 files): the first unit outside the math pilot — identidad digital, licenciamiento de software, servicios digitales, amenazas y medidas de seguridad digital.
- **`content/questions/pensamiento-matematico.ts`** (170 typed records): the complete question bank for all 34 pensamiento matemático lessons. every lesson has its five reserved stable ids represented; each record has exactly three options, an explanation, a plausible common error, a source reference, a difficulty and use cases. it includes direct, completion, relation and ordering shapes, and was mechanically validated against the lesson frontmatter on 2026-09-11.
- **`docs/tasks/2026-09-10-report_curriculum-mapping-guide.md`**: step-by-step instructions for turning the guide into curriculum data (steps 1–5: tree extraction → lesson breakdown → lesson content → question bank → validation). steps 1–2 are done for all 7 areas; step 3 is complete for pensamiento matemático and in progress for the other areas; steps 4–5 are complete for all pensamiento matemático units and have not started for other areas.
- **`docs/tasks/2026-09-10-report_lesson-blueprint.md`**: the copyable base structure for lesson files, with area-by-area content guidance (math/science = calculation-heavy worked examples; history/social/humanities = concept-contrast examples; lengua y comunicación's English subarea = actual English example sentences, Spanish scaffolding around them) and the "intenta primero / respuesta" convention for guided practice.
- **`docs/tasks/2026-09-10-report_project-understanding.md`**: the full history — initial analysis, every resolved contradiction, the curriculum-mapping review findings, and the pilot-lesson fix. Read this if you need the reasoning behind any decision above, not just the conclusion.
- **`docs/tasks/2026-09-11-report_question-bank-style-notes.md`**: running file of cross-cutting question-bank style observations from reviews (reactivo shape variety, difficulty tagging conventions). Single file, appended to after each future review — not a new dated file per session. Read before authoring or reviewing a new area/unit's question bank.
- **`docs/tasks/2026-09-15-report_product-decisions.md`**: running file of product/implementation decisions made during development that aren't already settled by `docs/*.md` (e.g. lesson completion is deliberately not gated on answering its comprobación). Single file, appended to as new decisions come up — not a new dated file per decision. Read before "fixing" behavior that might actually be a deliberate, already-decided tradeoff.
- **`docs/guiaoficial.pdf`**: the source of truth for curriculum scope. Pages 11–20 are the full area/subárea/tema table with official reactivo counts. **Pages 26–30 have the exam's actual reactivo format rules and a few real sample questions (one per area shown so far)** — these are the calibration reference for question format and for checking a lesson actually teaches what gets tested, not just what the compressed orientación text says. Worth reading further (pages 30+) as more areas' Step 3/4 work starts, since each area seems to get its own small sample-question block before its bibliografía.

## A concrete lesson already learned

`pm-tipos-de-variables-01.md`'s original draft only taught cuantitativa/cualitativa/categórica, matching the guide's orientación text — but the guide's own sample question for that exact tema tests cuantitativa **discreta vs. continua**, never mentioned in the orientación summary. Fixed now, but the general rule stands for every future lesson: check the sample-question pages for that topic before finalizing depth, don't just paraphrase the orientación column.

## Immediate next steps (in order)

1. verify the etapa-0 vercel preview if the repository has been connected. the local code and quality acceptance are complete; deployment remains an external action.
2. **post-etapa-5, task 1 complete**: `src/features/practice/pilot-review-candidates.ts`'s `get_pilot_review_candidates` (kept its legacy name/signature, per the task's own instruction not to do mass `pilot`-prefix renames) no longer depends on `get_pilot_curriculum()`/`get_pilot_topic_practice()`; it now iterates `available_units` (registry order) and each unit's `resolved.unit.topics` (curriculum order), resolving every topic through the existing generic `get_topic_content` and flattening its `topic_question[]` — so `/practica`'s review queue now covers all seven registered mvp areas, not just pensamiento matemático, and multi-lesson topics (`ch-3-1-2`) keep correct per-question lesson attribution because `get_topic_content` already paired each question with its own declaring lesson. `select_pending_review_items`, the attempt schema, storage and `PilotReviewQueue`'s interface were not touched. Unit tests cover the full-question-count across all seven areas, unchanged pm behavior, a non-pm area (cultura digital), per-question lesson attribution on the `ch-3-1-2` split topic, and registry/curriculum order as the stable sort. `e2e/navigation.spec.ts` gained one test: a wrong answer on a cultura digital topic appears in `/practica`, links to the right lesson, and clears on a correct retry. format check, lint, typecheck, the full unit suite (145 tests, up from 141), the production build (route list unchanged) and the full 42-test chromium e2e suite all pass. this incidentally makes home guidance's pending-review count cross-area too, as a side effect of fixing the shared resolver those pages already call — no change was made to home guidance, `/progreso`, or their own logic.
3. **post-etapa-5, task 2 complete — cross-area progress and home guidance**: `src/features/progress/global-progress.ts` is a pure module (no fs/server imports) with `calculate_global_progress(areas, progress)` (per-`available_units`-entry `topic_progress[]` plus a `calculate_pilot_summary`-derived per-area summary) and `get_global_next_action(areas, completed_lesson_ids)` (first non-`bloqueado`/non-`dominado` topic across areas, in `available_units` registry order then curriculum order, returning area/unit/topic/lesson ids plus `href`/`label`; `completed` only once every area's every topic is `dominado`). the async per-area resolution (`get_unit_lessons` + `create_pilot_topic_progress_definitions` per `available_units` entry) lives in a *separate* file, `global-topic-definitions.ts` — splitting it out was necessary, not stylistic: colocating the fs-based resolver with the pure functions in one module made turbopack panic (`does not support external modules (request: node:fs/promises)`) the moment a `"use client"` file imported anything from that module, since bundling pulls in the whole module including its `node:fs/promises`-importing chain. this mirrors the existing `pilot-lessons.ts` (fs) vs `pilot-progress.ts` (pure) split and is the reusable lesson for any future cross-area pure/async composition. `use-global-dashboard.ts` (client hook) and `global-home-guidance.tsx`/`global-next-action-cta.tsx` (new small components, home-only) consume the pure module; `app/page.tsx` now calls `get_global_topic_definitions()` + `get_pilot_review_candidates()` and renders `GlobalHomeGuidance`, with its heading generalized from the hardcoded "tu próxima acción en pensamiento matemático" to "tu próxima acción" (the CTA label/href themselves already carry the real next lesson/topic). the pm-only `PilotHomeGuidance` became dead code once `app/page.tsx` switched and was deleted, per the project's established no-shims convention; `PilotNextActionCta`, `get_pilot_next_action`, `calculate_pilot_summary`, `usePilotDashboard` and `PilotProgressSummary` (i.e. everything `/progreso`'s still-pm-only summary depends on) were deliberately left untouched — task 3 owns the multi-area dashboard. diagnóstico and simulacro's own helpers (`create_pilot_topic_progress_definitions`, `calculate_topic_progress`, `get_pilot_topic_practice`, `get_unit_topic_practice`) were not touched. Unit tests (`global-progress.test.ts`) cover: `get_global_topic_definitions` covering exactly the seven `available_units` entries in order and reproducing pm's existing per-unit computation; `calculate_global_progress` computing each area independently (fixture areas, not real content) and preserving area/topic order; `get_global_next_action`'s three required cases — pm dominated + another area intact recommends that area's first eligible action, a blocked topic is never recommended, and `completed` only once every fixture area is dominated. `e2e/navigation.spec.ts` gained one test seeding a fully-dominated pensamiento matemático `learner_progress` via `addInitScript` (20 correct attempts + 4 completed lessons, matching the `≥80%`/`≥5 attempts` mastery rule) with cultura digital untouched, asserting home does *not* say "completed" and instead links to `/leccion/cd-identidad-digital-01` ("continuar identidad digital") — the task's documented acceptance scenario. format check, lint, typecheck, the full unit suite (152 tests, up from 145), `pnpm build` (route list unchanged) and the full 43-test chromium e2e suite all pass. no route, persistence, threshold, diagnóstico, simulacro, auth, supabase or content change was made.
4. **post-etapa-5, task 3 complete — `/progreso` is now the multi-area dashboard, and post-etapa-5's unblocked work is done**: `src/app/progreso/page.tsx` now renders `GlobalProgressOverview`, which composes task 2's `get_global_topic_definitions()` with the cross-area review candidates and has no pm-only curriculum dependency. `GlobalProgressSummary` renders global dominated-topic, completed-lesson, accuracy and pending-review metrics plus the global next action. `AreaProgressBlocks` renders one compact card per `available_units` entry, in registry order, with its route link, metrics and action or completed state. the existing pure summary and action helpers are reused; no progress rules, persistence, routes, content, diagnosis, simulator, auth or supabase behavior changed. obsolete pm-only dashboard components were removed after their only caller moved to the global composition. the full per-topic detail remains at `/ruta/[area]`, so `/progreso` stays a compact cross-area dashboard. unit tests cover the seven-area empty state and isolation between areas; e2e covers persisted cultura digital progress visible after reload while pm remains unchanged. format check, lint, typecheck, 154 unit tests, build and 44 chromium e2e tests pass.
5. **product decisions accepted on 2026-09-18**: diagnóstico is brief and per area, entered through `/diagnostico`'s area selector and run at `/diagnostico/[area]`; it uses one existing `diagnostic` question per topic in the selected registered unit and returns an area-scoped result and first recommendation. simulacro is one combined `simulacro de cobertura mvp` at `/simulacro`: one existing `simulator` question per topic across the seven registered units, currently 36 questions, grouped by area then topic in the final report and explicitly not presented as a full official exam. implementation is split into `2026-09-18-task_post-etapa-5-diagnostico-por-area.md` and `2026-09-18-task_post-etapa-5-simulacro-cobertura.md`.
6. **`2026-09-18-task_post-etapa-5-diagnostico-por-area.md` complete — diagnóstico is now per area**: `/diagnostico` is a selector listing all seven `available_units` entries in registry order, each linking to `/diagnostico/[area]`; an unknown or unregistered area id returns `notFound()`. `src/features/diagnostic/area-diagnostic.ts`'s `get_area_diagnostic(area_id)` replaces the old fixed pm-only `get_pilot_diagnostic()`: it resolves the area through `get_available_unit_for_area`, then for every topic in curriculum order calls the existing generic `get_topic_content(area_id, unit_id, topic_id)` (`features/practice/unit-topic-content.ts`) and picks the first question in that topic's already-resolved question order whose `use_cases` includes `"diagnostic"` — topic correspondence, three options, a valid answer index and an explanation are already validated inside `get_topic_content`'s own resolution, so the resolver adds no duplicate validation. This reproduces pensamiento matemático's exact previous four-question selection (`pm-tv-001`, `pm-tm-001`, `pm-mtc-001`, `pm-md-003`) with no hardcoded map, and correctly attributes the multi-lesson topic `ch-3-1-2` to its first lesson (`ch-rpo-001` → `ch-resistencias-de-pueblos-originarios-01`) since `get_topic_content` already orders questions by lesson. `get_area_diagnostic` returns `undefined` for an unknown area instead of throwing. The rest of the diagnostic stack was renamed off its pm-only `pilot-` prefix, since nothing outside the diagnostic feature ever called it (unlike `get_pilot_topic_practice`, which simulator still uses and which this task left untouched): `diagnostic-recommendation.ts`'s `get_diagnostic_recommendation` (unchanged logic — first incorrect topic, else first non-`bloqueado` `topic_progress`, else the first topic), `diagnostic-check.tsx`'s `DiagnosticCheck` (unchanged one-question-at-a-time UI, no reveal before the last submit), `diagnostic-view.tsx`'s `DiagnosticView` (gained a required `area_title` prop so its heading/copy read "en {area}" instead of being hardcoded to pensamiento matemático) and `features/progress/diagnostic-session.tsx`'s `DiagnosticSession` (unchanged, still uses `usePilotProgress`'s existing generic `record_diagnostic_answer`, mode `"diagnostic"`, no new schema). `/diagnostico/[area]/page.tsx` resolves the area, 404s on failure, then composes `get_area_diagnostic` with `get_unit_lessons`/`create_pilot_topic_progress_definitions` for the recommendation's accessibility check, exactly mirroring `/ruta/[area]`'s resolution pattern. Unit tests (`area-diagnostic.test.ts`) cover: pensamiento matemático's unchanged four-question selection and lesson attribution; cultura digital's five-topic coverage; generic per-item validation (topic correspondence, three options, explanation, `diagnostic` tag); the `ch-3-1-2` multi-lesson attribution case; and an unknown area returning `undefined`. `diagnostic-recommendation.test.ts` keeps its three existing cases (first incorrect topic, first accessible topic skipping a blocked one, full-fallback when every topic is blocked) under the renamed types. `e2e/navigation.spec.ts` gained two tests — the selector showing all seven area links and navigating to `/diagnostico/cultura-digital`, and a full cultura digital diagnostic run whose result only shows its own five topics with no pensamiento-matemático text — plus every existing pm diagnostic e2e test (keyboard, full run, incorrect-answer recommendation, reload persistence) was repointed from `/diagnostico` to `/diagnostico/pensamiento-matematico` with no behavior change, and two new base-route smoke entries (`/diagnostico/pensamiento-matematico`, `/diagnostico/cultura-digital`) were added. format check, lint, typecheck, the full unit suite (156 tests, up from 154), `pnpm build` (route list gained exactly `/diagnostico/[area]`) and the full 46-test chromium e2e suite all pass. no persistence schema, mastery threshold, simulacro, review, dashboard, home guidance, auth, supabase or editorial content change was made; `get_pilot_topic_practice` (still used by the simulator) and `pilot-simulator*.ts` were not touched.
7. **`2026-09-18-task_post-etapa-5-simulacro-cobertura.md` complete — simulacro is now one combined cross-area coverage flow, and the post-etapa-5 assessment batch is fully done**: `/simulacro` no longer runs pensamiento matemático's 20-question pilot simulator; it runs one `simulacro de cobertura mvp` with exactly 36 questions, one `simulator`-tagged question per topic across all seven `available_units` entries, in registry order then curriculum order. `src/features/simulator/simulator-coverage.ts`'s `get_simulator_coverage()` iterates `available_units` and, for every topic in each unit, calls the existing generic `get_topic_content(area_id, unit_id, topic_id)` and picks the first question whose `use_cases` includes `"simulator"` — every authored question in every bank happens to carry the `simulator` tag, so this deterministically selects each topic's first question (confirmed against a full 36-item dump before writing fixtures). Beyond `get_topic_content`'s own topic/options/answer/explanation validation, the resolver adds `validate_simulator_question` (exported, unit-tested directly with hand-built fixtures rather than mocked content): it throws a clear, specific error if a resolved question isn't tagged `simulator`, is missing `common_error` or `source_reference`, or doesn't belong to its topic — and the top-level loop throws if a registered unit, topic or topic-simulator-question can't be resolved, or if two resolved questions share an id, so a broken registry entry fails loudly instead of silently shrinking the coverage list. This mirrors `diagnostic/area-diagnostic.ts`'s pattern exactly, extended to iterate every area instead of one. The report, check and session layers were renamed off their pm-only `pilot-simulator-*` names (nothing outside the simulator feature and its one page ever called them, so the old files were deleted outright, not kept as shims): `simulator-report.ts`'s `calculate_simulator_coverage_report` now groups by area first and by topic second (previously topic-only, since only one area existed) — each topic report still carries its own lesson, correct/total/accuracy and incorrect items, and area reports aggregate their topics' totals; `simulator-check.tsx`'s `SimulatorCheck` shows `área · tema` in its per-question heading and renders the results as area → topic, explicitly labelled "simulacro de cobertura mvp" both before starting (`simulator-view.tsx`'s `SimulatorView`) and in the finished report, with copy stating it is not the complete official exam; `features/progress/simulator-session.tsx`'s `SimulatorSession` is unchanged in behavior, still calling `usePilotProgress`'s existing generic `record_simulator_answer` (mode `"simulator"`, no new schema) — nothing about persistence, mastery or the attempt shape changed. Reloading `/simulacro` at any point still resets the visual session to question one while prior `simulator` attempts remain ordinary progress history. Unit tests (`simulator-coverage.test.ts`) cover: exactly 36 items grouped by area in `available_units` order; curricular topic order within an area; all seven areas present with pm's four topics intact; per-item validation (topic correspondence, `simulator` tag, three distinct options, valid answer index, explanation); unique question ids across the full 36; the `ch-3-1-2` multi-lesson topic attributed to its first lesson (`ch-rpo-001` → `ch-resistencias-de-pueblos-originarios-01`); and `validate_simulator_question`'s four failure cases plus its pass case, tested directly on constructed fixtures. `simulator-report.test.ts` (renamed from `pilot-simulator-report.test.ts`) covers area-level grouping/order/accuracy, topic order within an area, per-topic incorrect-item detail, lesson linkage and the zero-error case. `e2e/navigation.spec.ts` replaced every pm-only simulator test with three cross-area ones: a short interaction test asserting the "simulacro de cobertura mvp" label and its "no es el examen oficial completo" copy appear before starting, "pregunta 1 de 36", keyboard-driven no-reveal selection and no correctness text before the report; one full 36-question run (all 36 correct answers derived from a real resolved dump, not guessed) with one deliberate wrong answer forced on a non-pm topic (cultura digital's "tipos de servicios digitales", via a `hasNotText`-filtered label click rather than a hardcoded wrong-answer string) verifying the report shows "35 de 36 respuestas correctas", pm's own area block still "4 de 4 correctas", the failed area/topic's incorrect detail and its "repasar servicios digitales" link to `/leccion/cd-servicios-digitales-01`, then confirming the resulting `practica`... attempt appears as "1 intentos" on `/ruta/cultura-digital` and survives a `/simulacro` reload (which still resets to "pregunta 1 de 36"); and one mobile-viewport (375×667) interaction test. The three new/updated base-route entries needed no change (`/simulacro` was already in the smoke list). format check, lint, typecheck, the full unit suite (164 tests, up from 156), `pnpm build` (route list unchanged: `/diagnostico/[area]`, `/leccion/[lesson-id]`, `/practica/[topic-id]` and `/ruta/[area]` remain the only dynamic segments) and the full 45-test chromium e2e suite all pass; `git diff --check` is clean. no persistence schema, mastery threshold, diagnóstico, review, dashboard, home guidance, auth, supabase or editorial content change was made.

8. **`2026-09-18-task_diseno-editorial-fundamentos.md` complete — foundations, theme and shared frame**: `src/app/globals.css` replaced the old hardcoded `--background`/`--foreground` pair with the full semantic token set from `docs/16` (`--canvas`, `--surface`, `--surface-raised`, `--ink`, `--ink-muted`, `--line`, `--accent`/`-strong`/`-soft`/`-contrast`, `--success`/`--warning`/`--danger` plus soft variants, `--display-font`/`--body-font`/`--mono-font`), each pair chosen and verified to clear wcag aa (≥4.5:1) for every text/background combination used; a `@theme inline` block exposes the color tokens and `--font-display` as tailwind utilities (`bg-canvas`, `text-ink-muted`, `font-display`, etc.) without remapping tailwind's own `font-sans`/`font-mono` defaults, so untouched out-of-scope screens keep rendering exactly as before. dark mode is a `data-theme="dark"` attribute on `<html>`: `src/lib/theme/theme-preference.ts` (pure: `theme_preference` type, `is_theme_preference`, `resolve_theme`, `get_next_theme_preference`, cycle light → dark → system → light) and `theme-repository.ts` (a `ceneva.interface-theme` localstorage repository mirroring `progress-repository.ts`'s recovery-on-failure pattern) are the pure/storage layers; `theme-boot-script.ts` generates a small inline script, injected via `next/script strategy="beforeInteractive"` in `src/app/layout.tsx` (`<html suppressHydrationWarning>`), that reads the same storage key and sets `data-theme` before first paint with no flash — `theme-boot-script.test.ts` executes that exact generated string (via `new Function`) against the same matrix `theme-preference.test.ts` uses for `resolve_theme`, so the boot script's necessarily-duplicated raw-js logic can't silently drift from the typed logic. `src/components/layout/use-theme-preference.ts` (`useThemePreference`, camelCase per the codebase's existing hook-naming exception) applies the resolved theme via an isomorphic layout effect, re-reads the stored preference once mounted, and follows live system-preference changes while `preference === "system"`; `theme-toggle.tsx` is a native `<button>` (svg sun/moon/monitor icons, no icon library) cycling the three states, with an aria-label stating the current theme and the action ("tema: sistema. cambiar a claro."). `site-header.tsx` is now sticky, uses the semantic tokens, and gained the theme toggle plus a small "guía de estudio · acuerdo 286" descriptor next to the `Ceneva` brand mark (`font-display`); `site-footer.tsx` and the skip link use the same tokens. `src/lib/navigation.ts`'s five nav labels and the header/footer/metadata text were corrected to conventional spanish capitalization ("Inicio", "Mi ruta", "Práctica", "Simulacros", "Progreso", "Ceneva", "Acuerdo 286"); `navigation.test.ts` only asserts hrefs, so it needed no change, and every existing e2e link-name assertion still matches under playwright's case-insensitive substring rule. content screens (home, ruta, lecciones, práctica, diagnóstico, simulacro, progreso) were deliberately left untouched, including their hardcoded `slate-*`/`teal-*` classes — per the task's own scope boundary, this means those pages' dark-mode legibility (e.g. `text-slate-950` copy on the new dark canvas) is a known, real, and currently visible gap until the next redesign task migrates them to tokens; only the header/footer/skip-link/toggle are guaranteed legible in both themes per this task's acceptance criteria, and manual light/dark/mobile/desktop screenshots confirmed exactly that split. unit tests cover the new pure theme modules (theme-preference, theme-repository, theme-boot-script) with 17 new cases; `e2e/navigation.spec.ts` gained two tests (cycle + persistence + `ceneva.learner-progress` isolation; keyboard operation) for 47 total. format check, lint, typecheck, the full unit suite (181 tests, up from 164), `pnpm build` (route list unchanged) and the full 47-test chromium e2e suite all pass; `git diff --check` is clean. no dependency was added.

9. **task-1 dark-mode regression fixed with a temporary compatibility layer**: item 8 above shipped dark mode for the shared frame only, leaving every content screen's hardcoded `slate-*`/`teal-*`/`red-*`/`amber-*`/`emerald-*` tailwind classes illegible against the new dark canvas (e.g. `text-slate-950` copy reading as near-black on near-black). fixed entirely inside `src/app/globals.css`, no other file touched: a `:root[data-theme="dark"] { ... }` block at the end of the file overrides the exact utility-class selectors those screens already use (`.bg-white`, `.text-slate-950`, `.border-teal-700`, `.focus-visible\:outline-teal-700:focus-visible`, etc. — enumerated by grepping every `slate|teal|red|amber|emerald` class actually used across `src`), each redirected to the existing semantic tokens from item 8 (`var(--ink)`, `var(--surface-raised)`, `var(--accent)`, `var(--danger-soft)`, ...) rather than new hex values. globally remapping the underlying `--color-slate-*`/`--color-white` tailwind variables was rejected as unsafe, since e.g. `--color-white` also backs `text-white` on accent buttons, which must stay white in both themes; overriding the specific classes instead (verified against the compiled css that tailwind v4 generates each utility as `<utility> { <prop>: var(--color-<name>) }`) avoids that cross-role coupling entirely, so `bg-teal-700`/`hover:bg-teal-800` buttons (already self-contained with `text-white`) were deliberately left untouched. every new pairing was contrast-checked (reusing the same script/method as item 8) before use, and every decorative border's contrast was compared against its own original light-mode value to confirm no regression versus the shipped design's existing choices. this is explicitly temporary: the block is commented as such and should be deleted once the next redesign task migrates content screens to semantic tokens directly, at which point every selector in it should be dead code. manual verification (light/dark × desktop/mobile, via a fresh production build+server, since a stale `next start` process from the prior turn was initially serving 404ing assets and had to be killed) covered inicio, ruta, lección, práctica (including live correct/incorrect answer states), diagnóstico (question and full result screens), simulacro, progreso, the generic onboarding/recursos placeholders, and keyboard focus — all legible in both themes with light mode visually unchanged. no route, dependency, domain logic, or persistence change was made. format check, lint, typecheck, the full unit suite (181 tests, unchanged), `pnpm build` (route list unchanged) and the full 47-test e2e suite all still pass; `git diff --check` is clean.

10. **`2026-09-18-task_diseno-editorial-navegacion.md` complete — navigation and orientation surfaces redesigned**: inicio, `/ruta`, `/ruta/[area]`, `/progreso` and the root `/practica` review queue now use the semantic tokens directly (no more hardcoded `slate-*`/`teal-*`/`red-*`/`amber-*`/`emerald-*` classes) and the editorial index language from `docs/16` — numbered `01`/`02`… rows separated by thin `border-line` dividers instead of repeated `bg-white rounded-xl shadow-sm` cards, with `font-display` serif headings and small uppercase tracked labels for eyebrows/stat labels. three small generic presentational components were added to `src/components/ui/` (`Eyebrow`, `Stat`, `IndexList`/`IndexItem`) and are reused across all five screens rather than duplicating the pattern; `IndexItem` deliberately keeps the exact `<li><article>…</article></li>` shape the existing e2e suite already depends on via `xpath=ancestor::article` (48 occurrences), so no test needed structural rework beyond that. hardcoded chrome copy (headings, buttons, empty/loading states, action labels) was capitalized to conventional spanish sentence case; small labels (eyebrows, `dt` stat labels) keep their exact original lowercase text and get visual uppercase only through a css class, since several are asserted with `exact: true`/case-sensitive matchers (`"temas dominados"`, `"lecciones completadas"`) — this is a deliberate reading of docs/16 principle 6 ("versalitas... mediante estilo"), not an oversight. curriculum-derived text (area/unit/topic titles, `topic_status` values like `disponible`/`dominado`) was left untouched by design: it's domain data shared with the still-unmigrated learning-experience screens, and its capitalization is explicitly deferred to `2026-09-18-task_revision-editorial-capitalizacion.md`. per the task's own migration constraint, `globals.css`'s temporary dark-mode compatibility block (item 9) was pruned, not deleted: every selector that only the now-migrated screens used was removed after grepping `src` to confirm zero remaining references (`bg-slate-200`, `bg-teal-100`, `bg-teal-600`, `bg-amber-50`/`bg-amber-100`, `bg-emerald-100`, `hover:text-teal-800`, `text-amber-900`/`text-amber-950`/`text-emerald-950`, `border-amber-300`, `hover:border-amber-400`); every selector still used by lección, práctica por tema, diagnóstico, simulacro, onboarding or recursos was kept, and the block's own comment was updated to name the narrowed scope. one real pre-existing mobile cramping issue was fixed while rebuilding `AreaProgressBlocks` (not a capitalization/structure change, a genuine 320px legibility bug caught during manual verification): its three-stat `dl` was `grid-cols-3` with no responsive variant, causing "precisión" to wrap awkwardly at 320px; changed to `grid-cols-1 sm:grid-cols-3` so stats stack on mobile. two e2e assertions needed updating for the two literal chrome strings that changed case under case-sensitive matchers (`toContainText`/`toHaveText` are case-sensitive by default, unlike `getByRole`/`getByText`'s name-matching substring option, confirmed empirically by running the suite): `/ruta`'s h1 ("una ruta de estudio" → "Una ruta de estudio") and `/practica`'s h1 ("repasa tus errores pendientes." → "Repasa tus errores pendientes."). one new e2e test was added covering dark-theme persistence across `/ruta`, `/progreso`, `/practica` and a reload (task 1's own theme tests only covered inicio). format check, lint, typecheck, the full unit suite (181 tests, unchanged — no unit tests exist for these presentational components, only e2e), `pnpm build` (route list unchanged) and the full 48-test chromium e2e suite (up from 47) all pass; `git diff --check` is clean. no route, domain logic, persistence, curriculum order, mastery rule or editorial lesson/question content was touched.

the functional mvp is complete. the shared chrome/theme foundation and the navigation/orientation surfaces are both redesigned; the dark-mode compatibility layer in `globals.css` is narrowed but still real and still temporary — it remains load-bearing for lección, práctica por tema, diagnóstico, simulacro, onboarding and recursos until the next task migrates them. the next planned work is `docs/tasks/2026-09-18-task_diseno-editorial-aprendizaje.md` (learning experiences: lección, comprobaciones, práctica por tema, diagnóstico, simulacro), which should migrate those screens to the semantic tokens directly and then delete the remaining compatibility block entirely, followed by `2026-09-18-task_revision-editorial-capitalizacion.md`'s manual review of lesson/question-bank and curriculum-title capitalization.

11. **`2026-09-18-task_diseno-editorial-aprendizaje.md` complete — learning experiences redesigned, editorial redesign fully done**: `LessonView` and its markdown renderer, `LessonCheck`, `TopicPracticeCheck`/`TopicPracticeView`, the `/diagnostico` area selector, `DiagnosticCheck`/`DiagnosticView`, `SimulatorCheck`/`SimulatorView`, and every session wrapper's loading state (`lesson-session.tsx`, `topic-practice-session.tsx`, `diagnostic-session.tsx`, `simulator-session.tsx`) now use the semantic tokens (`text-ink`/`text-ink-muted`, `border-line`, `bg-surface`/`bg-surface-raised`, `bg-accent`/`text-accent-contrast`, `border-accent`/`bg-accent-soft`, `border-danger`/`bg-danger-soft`, `font-display` headings) instead of hardcoded `slate-*`/`teal-*`/`red-*` classes, matching the pattern task 2 already established (`Eyebrow`, `font-display` headings, no repeated card shadows). the `/diagnostico` selector was restructured to the same `IndexList`/`IndexItem` index pattern as `/ruta` (numbered rows, `border-line` dividers) instead of its old shadowed-card grid — safe because its e2e coverage asserts by role/link name, not by article structure. the per-question "check" components (`LessonCheck`, `TopicPracticeCheck`, `DiagnosticCheck`, `SimulatorCheck`) kept their exact existing DOM shape (`<li><article>`/`<fieldset>`/`role="status"` feedback boxes) since diagnostic/simulator e2e assertions depend on that structure via `xpath=ancestor::article`/`ancestor::li`; only their classes and copy changed. interface chrome was capitalized to conventional spanish sentence case (button labels, headings, loading/empty states, feedback labels like "Correcto."/"Incorrecto."/"Todavía no es correcto." — matching the capitalization `PilotReviewQueue` already established in task 2); question prompts, options, explanations and lesson markdown content were not touched, per docs/16's separate-review boundary. onboarding, recursos and the 404 page (plus the shared `PagePlaceholder` they both use) were also migrated to tokens and capitalized, completing the compatibility layer's documented remaining scope. one real pre-existing bug was found and fixed during e2e verification, unrelated to this task's own changes: two `/practica` review-queue e2e assertions (`un error de práctica por tema entra a la cola de repaso...` and its non-pm variant) asserted `toContainText("correcto.")` against `PilotReviewQueue`'s feedback box, which has rendered `"Correcto."` (capitalized) since task 2 — `toContainText` is case-sensitive (confirmed empirically), so these two tests had been silently failing since task 2 shipped; fixed by capitalizing the assertion text to match the component, no component change needed. with every hardcoded `slate-*`/`teal-*`/`red-*`/`amber-*`/`emerald-*` class gone from `src` (verified by a full grep before deleting), `globals.css`'s entire temporary dark-mode compatibility block (documented in item 9/10 above) was deleted outright — `globals.css` now ends after the `prefers-reduced-motion` block, with no theme-specific utility overrides left. `e2e/navigation.spec.ts` needed nine literal-string updates for case-sensitive `toHaveText`/`toContainText` assertions affected by the capitalization (not_found's heading in two tests, the simulator's h1 in two tests, and the "Correcto." feedback text in five tests plus the two pre-existing broken ones above); every other interaction in the suite uses `getByRole`/`getByText` name matching, which is case-insensitive substring by default (confirmed empirically), so it needed no change despite the wide capitalization pass. manual verification covered light/dark × desktop (1280px)/mobile (320px) screenshots of lección, práctica por tema (question and result states, including an incorrect answer), the diagnóstico selector and an area run (question and result states), simulacro, onboarding, recursos and not-found — all legible in both themes, with correct/incorrect states distinguished by both text and color, not color alone. format check, lint, typecheck, the full unit suite (181 tests, unchanged), `pnpm build` (route list unchanged) and the full 48-test chromium e2e suite all pass; `git diff --check` is clean. no domain logic, persistence, routes, question selection, reveal timing, keyboard behavior or pedagogical content changed.

12. **`2026-09-18-task_revision-editorial-capitalizacion.md` complete — the final content-capitalization review is done, and all editorial work described in this repo is now finished**: every lesson markdown file and every in-scope question-bank record of the seven registered units (37 lesson files; 185 question records — the full bank for the six single-unit areas, and only pensamiento matemático's 20 pm-1-1 records out of its 170-record file, since pm-1-2 through pm-1-6 are unregistered/invisible) was manually reviewed and corrected for conventional spanish capitalization, punctuation, proper nouns, institutions, places, historical periods and acronyms, together with the matching visible curriculum titles (area + registered unit + its topics) in all seven `content/curriculum/<area-id>.ts` files. the review ran as seven parallel area-scoped passes (one per area), each editing only its own disjoint files (lesson markdown, its slice of the question bank, its curriculum titles) — no cross-area conflicts. concept terms, units, formulas, variables and option fragments that are correctly lowercase in spanish were preserved; ids, routes, `topic_id`/`question_id`, frontmatter keys, official guide codes/pages, types, counts, correct answers and pedagogical intent were untouched everywhere (`git diff --stat` confirms exactly matching insertions/deletions, 1867/1867 — pure text edits, no structural change). two real regressions surfaced during the review and were fixed by the coordinator, not by content rewrites: (1) `lesson-view.tsx` splits a lesson body on the literal lowercase strings `"\n## comprobación\n"`/`"\n## cierre\n"` (see the note near the top of this file) — six of the seven area passes had capitalized those two headings along with every other one, which would have silently dropped those sections from 32 of the 37 lesson pages; reverted to lowercase across all affected files, every other heading (`Objetivo`, `Explicación`, `Ejemplo resuelto`, `Error frecuente`, `Práctica guiada`) stayed capitalized. (2) the corrected titles/explanations/option text made 18 unit tests and 25 e2e assertions fail on stale case-sensitive fixtures (`toEqual`/`toMatchObject` expected strings, and `toHaveText`/`toContainText`/`getByText(..., { exact: true })`, all case-sensitive, unlike `getByRole`/`getByText`'s default case-insensitive substring matching — same distinction documented in item 11 above); every stale fixture was updated to the corrected casing, including a few genuine proper-noun option-text fixes surfaced this way (e.g. río Yaqui vs. the yaqui people as an ethnonym, Usumacinta, Felipe Carrillo Puerto, la Danza del Venado) and one place where the fork's own e2e edit had drifted from its actual (correctly lowercase, grammatical-continuation-style) content and was reverted to match the content instead. format check, lint, typecheck, the full unit suite (181 tests, unchanged in count), `pnpm build` (route list unchanged) and the full 48-test chromium e2e suite all pass; `git diff --check` is clean. **flagged, not fixed (pre-existing, out of scope for a capitalization-only task)**: 32 of the 37 lesson files' `## comprobación` section still ends with a stale step-4 placeholder sentence ("sus enunciados, opciones, respuestas y explicaciones se incorporarán al banco tipado en el paso 4" or the cn equivalent) instead of real content, left over from before each area's question bank was authored — worth a real content pass, not a capitalization one.

13. **release-cleanup patch complete, applied on top of item 12's still-uncommitted editorial changes without discarding or splitting them**: two issues surfaced after item 12 were fixed. First, the `## comprobación`/`## cierre` code/content coupling described near the top of this file was resolved properly instead of worked around: `get_markdown_sections` now matches those two headings case-insensitively (see the note above), and the 37 registered mvp lesson files' headings were capitalized to `## Comprobación`/`## Cierre` to match every other section heading; the 30 unregistered legacy lesson files (pm-1-2 through pm-1-6) were left on lowercase headings, still supported by the same parser. Second, the stale future-tense placeholder sentence flagged in item 12 ("Sus enunciados, opciones, respuestas y explicaciones se incorporarán al banco tipado en el paso 4.") was replaced in the 32 affected lessons with the present-tense sentence already used by the other 34 lessons in the codebase ("Sus enunciados, opciones, respuestas y explicaciones están disponibles en \`content/questions/<area-id>.ts\`."), substituting each lesson's own `area-id`; nothing else in any `## comprobación` section (the "cinco reactivos" sentence, guide-calibration notes, page/code references) was touched. Verified by fetching a built lesson page (`/leccion/cs-necesidades-materiales-01`) and confirming the `Comprobación` heading, the interactive check (server-rendered `fieldset`/radio inputs), and `Cierre` render in that exact order with no stale sentence present. format check, lint, typecheck, the full unit suite (181 tests, unchanged), `pnpm build` (route list unchanged) and the full 48-test chromium e2e suite all pass; `git diff --check` is clean. no question meaning, id, frontmatter, guide-calibration note, formula, or other lesson content changed.

14. **complete second-pass interface redesign — responsive shell, contrast and learning controls**: the first editorial pass was functionally sound but remained too loose on mobile and too low-contrast in dark mode. the shared token system now has explicit soft surfaces, subtle text, strong lines and higher-contrast dark values; `main` owns its canvas explicitly so route transitions cannot expose a mismatched surface. the header is one 64px row on mobile (brand, theme, menu), with an accessible two-column menu panel, while desktop keeps a compact centered navigation group. home, route, diagnostic, progress, placeholders and the 404 page use shared page/reading shells and action styles. all lesson, practice, review, diagnostic and simulator question flows use the same progress meter, lettered native-radio options, clear selected/correct/incorrect states and structured feedback panels. the native inputs remain fully keyboard-accessible and cover their complete option row, which preserves both touch ergonomics and playwright's direct `radio.check()` contract. no route, content, domain, storage or assessment rule changed. format, lint, typecheck, all 181 unit tests, production build and all 49 chromium e2e tests pass; mobile and desktop were visually checked in both themes across home, route, lesson, practice, diagnostic, simulator and progress.

15. **pedagogical ux revision complete — lessons, questions and reports are now structured around learning state rather than generic text blocks**: the shared question ui separates optional source material (`stimulus`) from the actual instruction (`prompt`), contains both inside one border-safe question surface and preserves native `fieldset`/radio semantics. practice, review, diagnostic and simulator reports now state `acierto` or `por reforzar` explicitly and separate the learner's answer, correct answer, explanation and common error; total scores are visually primary, and long simulator reports collapse correct detail while keeping errors open. registered lessons no longer expose the internal `## comprobación` authoring/calibration notes; they render a consistent objective → aprende → observa → error frecuente → inténtalo → interactive check → cierre sequence with a desktop outline and compact activity headings. four long lengua y comunicación reactivos now use the optional typed `stimulus` field, validated by the shared question-bank boundary. no assessment selection, answer key, progress rule, route or reveal timing changed. the new e2e coverage includes a long mobile reading question and explicit result states.

16. **full curriculum expansion planned — content first, then auth/synchronization**: `docs/tasks/2026-09-23-report_expansion-de-contenido.md` is the source of truth for the audit and delivery gates. the complete curriculum has 34 units, 184 planned lessons and a final target of 920 questions. seven mvp units are visible today (37 lessons / 185 questions); pensamiento matemático already contains its other five units as 30 lesson files / 150 questions but the app cannot yet expose more than one unit per area. the six other areas require 117 new lessons and 585 new questions across 22 units. do not author or register those units opportunistically. first complete `2026-09-23-task_expansion-preparacion-multiunidad.md`: make areas own multiple ready units, group routes/progress by area, aggregate area diagnostics across ready units and add generic integrity coverage; register the pre-existing pensamiento matemático units only if that audit passes. then use `2026-09-23-task_expansion-contenido-por-area.md` for claude's unit-scoped authoring. mechanical validation is required after each unit; editorial calibration happens on the first new unit of each area, and a focused ui/e2e plus sample audit happens after each completed area. after all seven areas are complete, run the global content audit before starting etapa 4 auth/synchronization. no account, supabase, schema or progress migration work belongs to content expansion.

17. **`2026-09-23-task_expansion-preparacion-multiunidad.md` complete — the multi-unit foundation is done, and pensamiento matemático's other five units are registered.** the mechanical audit of `pm-1-2` through `pm-1-6` (real production resolvers — `get_unit_lessons`, `get_topic_content`, `find_invalid_options` — run against the actual content, not hand inspection) found zero contract violations across 30 lesson files and 150 question records: valid frontmatter, exact question-to-lesson/topic attribution, five reserved ids per lesson, and diagnostic/simulator coverage per topic. one real structural fact surfaced and was verified safe, not a bug: three `pm-1-6` lessons (`pm-derivadas-de-funciones-polinomiales-01`, `pm-graficas-de-funciones-01`, `pm-intervalos-y-desigualdades-01`) declare `pm-expresiones-algebraicas-01` (from `pm-1-3`) as a prerequisite — the first real cross-unit prerequisite in the app. it resolves correctly with no code change because `calculate_topic_progress` checks a prerequisite lesson id against the learner's global `completed_lesson_ids`, never against a single unit's own lesson set. `src/features/curriculum/available-units.ts` now has twelve entries: pensamiento matemático's six units, inserted contiguously and in curricular order right after `pm-1-1`, then the other six areas' one existing unit each — this keeps "registry order" and "curricular order within an area" the same invariant everywhere else in the codebase already relies on. `get_available_unit_for_area` (picked the first unit for an area) was deleted and replaced by `get_available_units_for_area` (`src/features/curriculum/available-curriculum.ts`), which returns every ready unit of an area in that same order; every caller that needs "the area's content" now iterates it instead of assuming one unit. routes: `/ruta` now dedupes `available_units` to one row per area, showing a ready-unit count ("6 unidades listas" for pensamiento matemático, "1 unidad lista" for the other six) and linking to `/ruta/[area]`; `/ruta/[area]` itself changed meaning — it is now a unit index (one row per ready unit, linking onward), not a topic list — and a new route `/ruta/[area]/[unit]` (reusing the existing `UnitRoute` component unchanged) renders one selected unit's topics, gaining a small "volver a las unidades de {área}" back-link. `/leccion/[lesson-id]` and `/practica/[topic-id]` were not touched — same urls, same behavior. this was a deliberate architecture choice (confirmed with the user before implementation, given its e2e blast radius): a uniform two-level `/ruta/[area]` → `/ruta/[area]/[unit]` split for every area, not a conditional shortcut for areas with only one unit, since most areas will eventually have several units too. diagnostic: `area-diagnostic.ts`'s `get_area_diagnostic` and `/diagnostico/page.tsx`'s selector now iterate `get_available_units_for_area` instead of one unit, so pensamiento matemático's diagnostic covers all 30 topics across its six units (was 4) in curricular unit order; every other area is unchanged (still one unit each). simulator: `simulator-coverage.ts` needed no change at all — it already iterated `available_units` and grouped by `resolved.area`, so adding six more entries that share the same `pensamiento_matematico` area object automatically extended pm's coverage from 4 to 30 topics inside the existing per-area group; total coverage grew from 36 to 62 questions. progress aggregation: `area_topic_definitions`/`area_progress` (`src/features/progress/global-progress.ts`) dropped their `unit_id`/`unit_title` fields (nothing downstream read them) since an area can now span several units; `global-topic-definitions.ts` gained `get_area_topic_definitions(area_id)`, which concatenates every ready unit's topic definitions into one area-level entry, and `get_global_topic_definitions()` now dedupes `available_units` by `area_id` before calling it — so `/progreso` and home guidance still show exactly one card per area (pensamiento matemático's card now aggregates all 30 topics/34 lessons), not one per unit. a new generic integrity suite, `src/lib/validation/registered-units.test.ts`, iterates every `available_units` entry (not just pensamiento matemático) and asserts curriculum-registry resolution, lesson loading with five reserved ids, exact question-to-lesson/topic attribution, diagnostic/simulator coverage per topic, structural question validity, and no duplicate lesson or question id across the full registered set (50 assertions across the twelve units); the existing per-area hardcoded-reserved-id blocks in `question-bank.test.ts` were kept as-is since they check something distinct (an explicit documented id table, plus specific relation/ordering-shape coverage). no question id, answer key, mastery threshold, storage schema, reveal timing or shared question ui changed; no non-mathematics content was authored. format check, lint, typecheck, 236 unit tests (up from 186), `pnpm build` (route list gained exactly `/ruta/[area]/[unit]`) and 53 chromium e2e tests (up from 49 — most of the ~45 existing tests that navigated straight to `/ruta/<area>` were mechanically repointed to `/ruta/<area>/<unit-id>`; the seven tests that click through `/ruta`'s "explorar {área}" link were restructured to add the new intermediate unit-selection click; three new tests cover the area-level unit index, the six-unit list, and a full lesson-to-`dominado` flow on a newly registered `pm-1-2` topic without affecting `pm-1-1` or another area) all pass; `git diff --check` is clean.

**registered-unit count after this task: 12** (6 pensamiento matemático + 1 each for the other six areas). **topic counts by area**: pensamiento matemático 30, cultura digital 5, conciencia histórica 5, humanidades 4, ciencias naturales/experimentales/tecnología 5, lengua y comunicación 4, ciencias sociales 9 — 62 topics total. **diagnostic coverage**: one question per topic per area, so pensamiento matemático 30 and every other area unchanged (5/5/4/5/4/9). **simulator coverage**: 62 questions total (30 + 32), grouped by area then curricular topic order, up from 36. **unresolved risks**: none found in the audited content or the new registry/route/progress code; the only pre-existing, out-of-scope gap is that `pm-1-2` through `pm-1-6`'s curriculum unit/topic titles (in `content/curriculum/pensamiento-matematico.ts`) and lesson bodies were never run through the 2026-09-18 capitalization pass that `pm-1-1` and the other six areas received (they use lowercase titles like "pensamiento probabilístico"), since this task's scope was structural, not editorial — worth folding into a future editorial pass, not a correctness issue today.

18. **`2026-09-23-task_revision-editorial-pm-expandido.md` complete — the expanded pensamiento matemático editorial gate is done.** the five newly visible units (`pm-1-2` through `pm-1-6`) now match the manual spanish capitalization/punctuation standard already applied to the mvp content: 31 curriculum title records (5 unit titles, 26 topic titles, including the two acronym titles `Mínimo común múltiplo (MCM)`/`Máximo común divisor (MCD)`) in `content/curriculum/pensamiento-matematico.ts`, all 30 lesson files (headings, frontmatter `title`/`objective`, body prose — now capitalized `## Comprobación`/`## Cierre` like the other 37 registered lessons), and all 150 question records in `content/questions/pensamiento-matematico.ts` (575 field-level edits to `prompt`/`explanation`/`common_error`/`source_reference` across every one of the 150 records). single-letter and short-token math notation (`x`, `m`, `c`, `r`, `t`, `p(a | b)`, `lim`, `cos(x)`, `ln(x)`, function/variable names inside numbered relación/jerarquización list items) was deliberately kept lowercase even where it opened a sentence or a list item, matching the existing convention (e.g. `pm-teorema-de-pitagoras-01`'s `` `a` y `b` son los catetos``) that deliberate mathematical symbols are preserved exactly, never capitalized as if they were prose words. `/diagnostico`'s selector copy no longer promises a uniformly brief flow — it now states that length varies with each area's ready topic coverage. no id, answer key, objective, prerequisite, question count, source reference, evaluation rule or selection logic changed; `git diff` on the four structural fields (`id`, `topic_id`, `options`, `correct_option_index`, `difficulty`, `use_cases`) across all 150 records is empty, confirmed mechanically. two literal e2e fixtures (a `toEqual` array of unit headings and two `toHaveText` heading assertions) needed updating for the new casing, all under the already-existing pm-1-2 registration test — no new test was required. format check, lint, typecheck, the full unit suite (236 tests, unchanged), `pnpm build` (route list unchanged) and the full 53-test chromium e2e suite all pass; `git diff --check` is clean. once this gate passes, start the first non-mathematics calibration unit: `cd-2-2-comunicacion-y-colaboracion-digital`.

19. **`cd-2-2-comunicacion-y-colaboracion-digital` complete — cultura digital's calibration unit is registered as the area's second unit, prompt 1 of 22 in the expansion queue.** its four topics (`2.2.1` definición del ciberespacio, `2.2.2` definición de TICCAD, `2.2.3` función y uso de herramientas digitales, `2.2.4` métodos de investigación digital) and five lessons — `cd-ciberespacio-01`, `cd-ticcad-01`, `cd-funciones-de-herramientas-digitales-01`, `cd-uso-de-herramientas-digitales-02`, `cd-metodos-de-investigacion-digital-01` — match the source map exactly, including `2.2.3`'s documented two-lesson split (a general function-to-need mapping, then selecting among the nine named tools prezi, genially, drive, zoom, meet, docs, kahoot, sites, canva). `content/questions/cultura-digital.ts` gained 25 new records (five per lesson, ids `cd-ce-*`/`cd-tc-*`/`cd-fh-*`/`cd-uh-*`/`cd-mi-*`), for 50 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 12 with its `2.2.x` code, and every topic has diagnostic and simulator coverage. `content/curriculum/cultura-digital.ts`'s `cd-2-2` unit and topic titles were written in conventional spanish capitalization directly (the calibration review this unit called for), unlike `pm-1-2`–`pm-1-6`, which needed a later separate editorial-gate task. none of area 2's three official guide sample reactivos (page 29: medidas de seguridad, already calibrated in `cd-2-1`, and lenguaje algorítmico; page 30: pasos de un algoritmo) belong to `2.2.x`, so no sample-calibration note was needed — matching the precedent already set by `lc-6-1` when its area's samples belonged to a different subárea. `src/features/curriculum/available-units.ts` registers `cultura-digital`/`cd-2-2-comunicacion-y-colaboracion-digital` as the registry's 13th entry, right after `cd-2-1`. registering a second unit for an already-visible area surfaced real cross-cutting assumptions baked into earlier single-unit-per-area tests and copy, all fixed in this same turn: cultura digital's topic count went from 5 to 9, so `/diagnostico/cultura-digital` is now 9 questions (was 5) and the cross-area `/simulacro` coverage resolver (which iterates every registered unit in order) grew from 62 to 66 questions, shifting every question after cultura digital's block in the flat coverage sequence; `/ruta`'s unit-count badge for cultura digital changed from "1 unidad lista" to "2 unidades listas"; and `/progreso`'s cultura digital dominated-topics stat changed from a fixed `/5` to `/9`. All five new lessons' diagnostic-tagged opening question originally had its correct answer at option index 0 in every single lesson — a predictable pattern that also happened to break an existing e2e test that deliberately picks the first rendered radio button to force a wrong answer; both were fixed by reordering those five questions' options so the correct index varies (1, 2, 1, 2, 1), matching the variety already present in `cd-2-1`. format check, lint, typecheck, the full unit suite (247 tests, up from 236), `pnpm build` (route list unchanged) and the full 55-test chromium e2e suite all pass; `git diff --check` is clean. next in the queue is `cd-2-3-creatividad-digital`.

20. **`cd-2-3-creatividad-digital` complete — cultura digital's third unit, prompt 2 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; instead it confirmed item 19's calibration passed (typecheck clean, 247/247 unit tests green before starting) and applied its two durable findings directly while authoring. its five topics (`2.3.1` herramientas de hoja de cálculo, `2.3.2` herramientas de procesamiento de texto, `2.3.3` herramientas de presentaciones electrónicas, `2.3.4` fases del método addie, `2.3.5` campos de aplicación de herramientas tecnológicas) and five single-lesson topics — `cd-formulas-de-hoja-de-calculo-01`, `cd-herramientas-de-procesamiento-de-texto-01`, `cd-herramientas-de-presentaciones-01`, `cd-metodo-addie-01`, `cd-campos-de-aplicacion-tecnologica-01` — match the source map exactly, verified again directly against `docs/guiaoficial.pdf` page 13. `content/questions/cultura-digital.ts` gained 25 new records (five per lesson, ids `cd-hc-*`/`cd-pt-*`/`cd-pe-*`/`cd-ma-*`/`cd-ca-*`), for 75 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 13 with its `2.3.x` code, at least one relation question per lesson (except the addie lesson, which uses an ordering/jerarquización question instead — sequencing the five addie phases is a genuinely procedural fit, per the running style notes), and every topic has diagnostic and simulator coverage. `content/curriculum/cultura-digital.ts`'s `cd-2-3` unit and topic titles, previously an unregistered lowercase stub left over from the multi-unit foundation task, were capitalized to conventional spanish directly in this task (durable finding 1 from item 19's calibration). durable finding 2 from item 19 (the first, diagnostic+simulator-tagged question of every new lesson had landed at option index 0 in every single lesson, a predictable pattern) was applied proactively this time: every lesson's opening question has its correct option away from index 0, deliberately varied across index 1 and index 2 (not settled into a new fixed index) so no new predictable pattern replaces the old one. checked again directly against `docs/guiaoficial.pdf` pages 29–30: none of area 2's three official sample reactivos (page 29 medidas de seguridad, already calibrated in `cd-2-1`, and lenguaje algorítmico; page 30 pasos de un algoritmo) belong to `2.3.x` either, so no sample-calibration note was needed. `src/features/curriculum/available-units.ts` registers `cultura-digital`/`cd-2-3-creatividad-digital` as the registry's 14th entry, right after `cd-2-2`; no new dedicated `describe` block was added to `question-bank.test.ts` for this unit (that treatment is reserved for calibration units, per the precedent already set by `pm-1-2`–`pm-1-6`), so its structural integrity is covered entirely by the generic `registered-units.test.ts` suite, which needed no edits to pick it up. registering a third unit for cultura digital again surfaced cross-cutting assumptions, all fixed in the same turn: cultura digital's topic count went from 9 to 14, so `/diagnostico/cultura-digital` is now 14 questions (was 9) and the cross-area `/simulacro` coverage resolver grew from 66 to 71 questions; `/ruta`'s unit-count badge for cultura digital changed from "2 unidades listas" to "3 unidades listas"; `/progreso`'s cultura digital dominated-topics stat changed from a fixed `/9` to `/14`. Two test fixtures that had used still-unregistered `cd-2-3-creatividad-digital`/`cd-formulas-de-hoja-de-calculo-01` ids as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) were repointed to the still-unregistered `cd-2-4-pensamiento-algoritmico`/`cd-conceptos-del-lenguaje-algoritmico-01` instead. format check, lint, typecheck, the full unit suite (251 tests, up from 247), `pnpm build` (route list unchanged) and the full 55-test chromium e2e suite all pass; `git diff --check` is clean. next in the queue is `cd-2-4-pensamiento-algoritmico`, which completes cultura digital and therefore also runs the full completed-area gate from the contract.

21. **`cd-2-4-pensamiento-algoritmico` complete — cultura digital's fourth and final unit, prompt 3 of 22 in the expansion queue, completing the area.** its four topics (`2.4.1` conceptos del lenguaje algorítmico, `2.4.2` pasos de un algoritmo, `2.4.3` características del pensamiento algorítmico, `2.4.4` símbolos de los diagramas de flujo en algoritmos) and four single-lesson topics — `cd-conceptos-del-lenguaje-algoritmico-01`, `cd-pasos-de-un-algoritmo-01`, `cd-caracteristicas-del-pensamiento-algoritmico-01`, `cd-simbolos-de-diagramas-de-flujo-01` — match the source map exactly, verified directly against `docs/guiaoficial.pdf` page 13; the real prerequisite chain (conceptos → pasos and conceptos → características; pasos → símbolos) was already documented in the source map's plan de lecciones and needed no new code, only registration. `content/questions/cultura-digital.ts` gained 20 new records (five per lesson, ids `cd-cla-*`/`cd-pa-*`/`cd-cpa-*`/`cd-sdf-*`), for 95 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 13 with its `2.4.x` code, at least one relation question in `cd-2-4-1` and one ordering/jerarquización question in `cd-2-4-2`, and every topic has diagnostic and simulator coverage. unlike `cd-2-2`/`cd-2-3`, this unit's two topics `2.4.1` and `2.4.2` directly match two of area 2's three official guide sample reactivos (page 29: a relación de elementos exercise on estructuras condicionales/operadores relacionales/operadores lógicos, matching `2.4.1`; page 30: a jerarquización exercise ordering the steps of an averaging algorithm, matching `2.4.2`) — both lessons' `## Comprobación` sections document this calibration boundary and each topic's bank includes an equivalent reactivo shape (relación for `cla-005`, ordering for `pa-005`) without copying the guide's wording; `2.4.3`/`2.4.4` have no matching sample. `content/curriculum/cultura-digital.ts`'s `cd-2-4` unit and topic titles, previously an unregistered lowercase stub, were capitalized to conventional spanish directly in this task. `src/features/curriculum/available-units.ts` registers `cultura-digital`/`cd-2-4-pensamiento-algoritmico` as the registry's 15th entry, right after `cd-2-3`; no new dedicated `describe` block was added to `question-bank.test.ts` (same precedent as `cd-2-3`), covered entirely by the generic `registered-units.test.ts` suite. registering cultura digital's fourth and closing unit surfaced the same cross-cutting assumptions as every prior unit in this area, all fixed in the same turn: cultura digital's topic count went from 14 to 18, so `/diagnostico/cultura-digital` is now 18 questions (was 14) and the cross-area `/simulacro` coverage resolver grew from 71 to 75 questions; `/ruta`'s unit-count badge for cultura digital changed from "3 unidades listas" to "4 unidades listas"; `/progreso`'s cultura digital dominated-topics stat changed from a fixed `/14` to `/18`. Since cultura digital is now fully registered (all 4 of its source-map units), the fixtures in `available-curriculum.test.ts`, `available-units.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts` that had used `cd-2-4-pensamiento-algoritmico`/`cd-conceptos-del-lenguaje-algoritmico-01` ids as examples of *unregistered* content were repointed to other areas' still-unregistered calibration units (`hu-4-2-elementos-para-el-pensamiento-y-la-argumentacion`, `ch-3-2-mexico-durante-el-expansionismo-capitalista`, `cn-5-2-conservacion-de-la-energia-y-sus-interacciones`). per the completed-area gate, one focused e2e test was added (not just fixture/route updates): completing `cd-conceptos-del-lenguaje-algoritmico-01` unlocks both dependent topics, a five-question topic practice on `cd-2-4-1` reaches `dominado`, and the result survives reload without affecting pensamiento matemático. format check, lint, typecheck, the full unit suite (255 tests, up from 251), `pnpm build` (route list unchanged) and the full 56-test chromium e2e suite all pass; `git diff --check` is clean. cultura digital is now the second fully-authored area (after pensamiento matemático). next in the queue is `hu-4-2-elementos-para-el-pensamiento-y-la-argumentacion`, humanidades' calibration unit.

the functional mvp, original content review and pedagogical ux revision are complete. the multi-unit foundation and its pensamiento matemático editorial gate are also done. active work is the 22-unit sequential queue in `2026-09-23-report_expansion-prompt-queue.md`; prompts 1–3 completed cultura digital; prompts 4–8 (`hu-4-2` through `hu-4-6`) completed humanidades, ending with `hu-4-6-reflexiones-sobre-el-arte-y-la-sensibilidad`'s completed-area gate; prompts 9–10 (`cs-7-2`, `cs-7-3`) completed ciencias sociales, ending with `cs-7-3-problemas-sociologicos`'s completed-area gate (item 28); prompts 11–12 (`ch-3-2`, `ch-3-3`) completed conciencia histórica, ending with `ch-3-3-realidad-actual-en-perspectiva-historica`'s completed-area gate (items 29–30); prompt 13, `cn-5-2-conservacion-de-la-energia-y-sus-interacciones`, ciencias naturales' calibration unit, is done (item 31); prompt 14, `cn-5-3-ecosistemas-interacciones-energia-y-dinamica`, is done (item 32); prompt 15, `cn-5-4-reacciones-quimicas-y-conservacion-de-la-materia`, is done (item 33); prompt 16, `cn-5-5-energia-en-los-procesos-de-la-vida-diaria`, is done (item 34); prompt 17, `cn-5-6-organismos-estructura-y-procesos`, is also done (item 35); next is prompt 18, `cn-5-7-herencia-y-evolucion-biologica`, ciencias naturales' area-closing unit, which requires the full completed-area gate. every unit keeps its mechanical gate, while `2026-09-23-task_expansion-auditoria-global.md` performs the final complete 34-unit, 184-lesson and 920-question audit and correction. auth and synchronization begin only after that audit returns `ready`. per the user's 2026-09-24 instruction, from `cs-7-3` onward playwright/e2e is skipped per unit and per area completion — it runs only once, during the global audit, unless a task changes shared production code, routes, progress, diagnostic, simulator or learning ui behavior. the only unrelated open item from earlier in this file is etapa 0's vercel-preview acceptance, which is an external action (connecting the repo to vercel), not code.

27. **`cs-7-2-perspectivas-politicas` complete — ciencias sociales' calibration unit, prompt 9 of 22 in the expansion queue.** its eight topics (`7.2.1` teorías sobre el origen del estado y sus autores, `7.2.2` características de la democracia electoral, `7.2.3` requisitos para obtener la ciudadanía mexicana, `7.2.4` función de las instituciones del estado mexicano, `7.2.5` poderes fácticos, `7.2.6` principios de política exterior del estado mexicano, `7.2.7` organismos internacionales, `7.2.8` áreas en el sistema mundo) and eight single-lesson topics — `cs-teorias-sobre-el-origen-del-estado-01`, `cs-democracia-electoral-01`, `cs-ciudadania-mexicana-01`, `cs-instituciones-del-estado-mexicano-01`, `cs-poderes-facticos-01`, `cs-principios-de-politica-exterior-01`, `cs-organismos-internacionales-01`, `cs-areas-en-el-sistema-mundo-01` — match the source map exactly, including its internal prerequisite chain (teorías del estado → instituciones del estado mexicano; teorías del estado → democracia electoral → ciudadanía mexicana; the other four topics have no prerequisite). `content/questions/ciencias-sociales.ts` gained 40 new records (five per lesson, ids `cs-toe-*`/`cs-dem-*`/`cs-ciu-*`/`cs-iem-*`/`cs-pfa-*`/`cs-ppe-*`/`cs-oin-*`/`cs-asm-*`), for 85 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 19 with its `7.2.x` code, and every topic has diagnostic and simulator coverage. this is ciencias sociales' calibration unit, so the durable findings already established for every other area's calibration unit were applied from the start: `content/curriculum/ciencias-sociales.ts`'s `cs-7-2` unit and topic titles, previously an unregistered lowercase stub left over from the multi-unit foundation task, were capitalized to conventional spanish directly in this task; each lesson's opening diagnostic+simulator question has its correct option away from index 0 (varied across index 1 and index 2). checked all three of ciencias sociales' official guide sample reactivos directly against `docs/guiaoficial.pdf` pages 42–44: the first (página 42, relación de elementos on necesidades vitales/no vitales) had already calibrated `cs-7-1-1` in the original mvp content, and the third (página 44, relación de elementos on tipos de organización social) belongs to the still-unregistered `cs-7-3-1`; only the second (página 43, a flooding scenario asking which institution attends it, correct answer Cenapred) falls inside this unit's scope, and it calibrates `7.2.4` even though its own correct institution (Cenapred, protección civil) is not one of the four the topic's official orientación names (SEP, CONADIS, SEDATU, INAES) — `cs-instituciones-del-estado-mexicano-01`'s `## Comprobación` documents this exactly (the sample demonstrates the general institution-matching skill the topic tests, but the lesson's own bank stays scoped to the four officially named institutions) and a dedicated question-bank test (`question-bank.test.ts`) asserts every `cs-iem-*` question names one of those four institutions and none mentions Cenapred. this unit deliberately does not use current political facts: `cs-poderes-facticos-01` uses generic actors (a media consortium, a business chamber, a union, a religious organization) rather than real named entities; `cs-principios-de-politica-exterior-01` uses hypothetical "país A"/"país B" scenarios rather than real geopolitical disputes; `cs-areas-en-el-sistema-mundo-01` classifies regions by abstract economic characteristics rather than naming real current countries as central/periférico/semiperiférico. per the question-bank-style-notes precedent of exercising jerarquización where a topic has a genuine procedure, `cs-democracia-electoral-01`'s advanced question orders the five stages of an electoral process, instead of the relación-de-elementos shape every other cs-7-2 lesson uses for its advanced question; the ciencias sociales question-bank test suite was updated accordingly (the generic "relation question per lesson" check now excludes `cs-7-2-2` and a separate test confirms its ordering question). `src/features/curriculum/available-units.ts` registers `ciencias-sociales`/`cs-7-2-perspectivas-politicas` as the registry's 21st entry, right after `cs-7-1`; `question-bank.test.ts`'s single `cs-7-1`-only describe block (a pre-multi-unit holdover that still asserted the whole bank's length) was narrowed to its own 45-question subset, matching every other area's established per-unit-block pattern, and a new dedicated `cs-7-2` describe block was added. registering ciencias sociales' second unit surfaced the same cross-cutting assumptions as every other area's calibration unit, all fixed in the same turn: ciencias sociales' topic count went from 9 to 17, so the cross-area `/simulacro` coverage resolver grew from 91 to 99 questions, shifting every question after ciencias sociales' block (the last area in `available_units`) in the flat coverage sequence — the hardcoded e2e `simulator_coverage_correct_options` fixture and every `pregunta X de 91`/`90 de 91` assertion were updated to 99/98; `/diagnostico/ciencias-sociales` and `/ruta/ciencias-sociales`'s unit count are not asserted with hardcoded figures anywhere, so neither needed a fixture change. test fixtures that had used `cs-7-2-perspectivas-politicas`/`cs-7-2-1-teorias-sobre-el-origen-del-estado`/`cs-teorias-sobre-el-origen-del-estado-01` ids as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`, including two pre-existing accidental duplicate assertions of the same id found while fixing this) were repointed to the still-unregistered `cs-7-3-problemas-sociologicos`/its topics/lessons. new unit tests cover the registry's 21st entry (`available-units.test.ts`, 85-question ciencias sociales bank), `get_available_unit` resolving cs-7-2's eight topics and `get_available_units_for_area` now returning two ciencias sociales units (`available-curriculum.test.ts`), `get_topic_content` for a real cs-7-2 topic (`unit-topic-content.test.ts`), and `get_unit_lessons` for all eight cs-7-2 lessons with their real prerequisite chain (`unit-lessons.test.ts`). format check, lint, typecheck, the full unit suite (340 tests, up from 323), `pnpm build` (route list unchanged) and the 12 affected chromium e2e tests (diagnostic selector, full simulacro coverage with the appended cs-7-2 answers, ciencias sociales route/practice) all pass; `git diff --check` is clean. no new e2e test was added, matching the precedent set by every other area's own calibration unit (cd-2-2, hu-4-2), since a dedicated e2e gate is only required at a completed-area close. next in the queue was `cs-7-3-problemas-sociologicos`, ciencias sociales' area-closing unit — see item 28.

28. **`cs-7-3-problemas-sociologicos` complete — ciencias sociales' third and final unit, prompt 10 of 22 in the expansion queue, completing the area.** not a calibration unit, so no new editorial gate; it confirmed item 27's calibration held (format check, lint, typecheck and the full unit suite were green before starting) and applied its approved findings directly. its eight topics (`7.3.1` tipos de organización social, `7.3.2` indicadores de desarrollo comunitario, `7.3.3` indicadores de bienestar, `7.3.4` factores que propician la segregación social, `7.3.5` derechos de niñas, niños y adolescentes, `7.3.6` consecuencias de las crisis sociales, económicas y ambientales de los siglos XX y XXI, `7.3.7` tipos de migraciones, `7.3.8` movimientos sociales antisistema) and eight single-lesson topics — `cs-organizacion-social-01`, `cs-indicadores-de-desarrollo-comunitario-01`, `cs-indicadores-de-bienestar-01`, `cs-segregacion-social-01`, `cs-derechos-de-ninas-ninos-y-adolescentes-01`, `cs-crisis-sociales-economicas-y-ambientales-01`, `cs-tipos-de-migraciones-01`, `cs-movimientos-sociales-antisistema-01` — match the source map exactly, including its internal prerequisite graph (organización social unlocks indicadores de desarrollo comunitario, indicadores de bienestar and segregación social; the other four topics have no prerequisite). `content/questions/ciencias-sociales.ts` gained 40 new records (five per lesson, ids `cs-tos-*`/`cs-idc-*`/`cs-idb-*`/`cs-seg-*`/`cs-dnna-*`/`cs-cse-*`/`cs-mig-*`/`cs-msa-*`), for 125 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 20 with its `7.3.x` code, and every topic has diagnostic and simulator coverage. of ciencias sociales' three official guide sample reactivos (pages 42–44), the third (página 44, relación de elementos matching family/community/social class to their definitions) falls inside this unit's scope, calibrating `7.3.1` exactly as item 27 flagged in advance — `cs-organizacion-social-01`'s `## Comprobación` documents that the sample uses "familia" as an example while the topic's actual four official categories are clase social, grupo social, comunidad e institución, so the lesson's bank evaluates those four with original definitions rather than reusing "familia" as its own category. two of this unit's topics required care to avoid current political facts, per the ongoing ciencias sociales restraint: `cs-crisis-sociales-economicas-y-ambientales-01` (Plan de San Luis, crisis de 1982, movimiento estudiantil de 1968, EZLN) sticks to neutral, textbook-level characterizations of settled 20th-century history, stating only the general type of social consequence each event had, with no partisan framing; `cs-movimientos-sociales-antisistema-01` (feminist, anti-globalization and hippie movements) describes each by broadly agreed general characteristics, situating the hippie movement explicitly in the 1960s–70s, without taking a position on any current-day demand. a real authoring mistake was caught during the mechanical gate: three of the eight lessons' opening diagnostic+simulator questions (`cs-idb-001`, `cs-dnna-001`, `cs-mig-001`) were drafted with their correct answer at index 0, violating the durable index-variety finding applied throughout every other calibration/near-calibration unit since `cd-2-3`; all three were fixed by reordering their options (now index 1, alternating with index 2 for the other four lessons, `cs-tos-001` also at index 1) before any test ran, so the shipped content follows the established convention correctly. `src/features/curriculum/available-units.ts` registers `ciencias-sociales`/`cs-7-3-problemas-sociologicos` as the registry's 22nd entry, right after `cs-7-2`; `question-bank.test.ts` gained a dedicated `cs-7-3` describe block (including a jerarquización check for `cs-segregacion-social-01`'s estereotipo→discriminación→exclusión ordering question, a relation-question check excluding that same lesson, the página-44 calibration check, and a lightweight no-current-political-facts check on the crisis/movements lessons). since ciencias sociales is now fully registered (all 3 of its source-map units), the "no resuelve una unidad de ciencias sociales todavía sin contenido listo" test in `available-curriculum.test.ts` was replaced with a real resolved-unit test for `cs-7-3`, and every fixture in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts` (including two duplicate-id assertions left over from a prior turn's fix) that had used `cs-7-3-problemas-sociologicos`/its topics/`cs-organizacion-social-01`/`cs-tipos-de-migraciones-01` as examples of *unregistered* content were repointed to conciencia histórica's still-unregistered `ch-3-2-mexico-durante-el-expansionismo-capitalista` and its topics/lessons. registering ciencias sociales' closing unit surfaced the same cross-cutting assumption as every other area's closing unit: ciencias sociales' topic count went from 17 to 25 (matching the source map's own validation total), so the cross-area `/simulacro` coverage resolver grew from 99 to 107 questions — the hardcoded e2e `simulator_coverage_correct_options` fixture and every `pregunta X de 99`/`98 de 99` assertion were updated to 107/106 in the same turn, even though playwright itself was not run this turn (see below). **this is the first unit run under the updated validation cadence the user set on 2026-09-24**: per unit, only focused registered-content integrity tests, the area's question-bank tests, typecheck and `git diff --check`; at a completed-area close, additionally format check, lint, the full unit suite and `pnpm build` — but playwright is skipped entirely, even at area completion, unless the task changes shared production code/routes/progress/diagnostic/simulator/learning-ui behavior (this one didn't, so no e2e test was added and none was run). format check, lint, typecheck, the full unit suite (357 tests, up from 340), `pnpm build` (route list unchanged) and `git diff --check` all pass; the e2e spec file's fixtures were kept current for the eventual single full run at the global audit, but playwright itself was not executed. ciencias sociales is now the fourth fully-authored area (after pensamiento matemático, cultura digital and humanidades). next in the queue is `ch-3-2-mexico-durante-el-expansionismo-capitalista`, conciencia histórica's calibration unit.

22. **`hu-4-2-elementos-para-el-pensamiento-y-la-argumentacion` complete — humanidades' calibration unit, prompt 4 of 22 in the expansion queue.** its four topics (`4.2.1` funciones de la lengua, `4.2.2` premisas y conclusión del argumento, `4.2.3` argumentos deductivos, inductivos y analógicos, `4.2.4` tipos de discurso argumentativo) and four lessons — `hu-funciones-de-la-lengua-01`, `hu-premisas-y-conclusion-01`, `hu-tipos-de-argumentos-01`, `hu-discursos-argumentativos-01` — match the source map exactly, including its genuine linear prerequisite chain (each lesson requires the previous one), unlike `hu-4-1`'s single-foundation-plus-three-independents shape. `content/questions/humanidades.ts` gained 20 new records (five per lesson, ids `hu-fdl-*`/`hu-pyc-*`/`hu-tda-*`/`hu-dar-*`), for 40 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 15 with its `4.2.x` code, at least one relation question per lesson, and every topic has diagnostic and simulator coverage. of humanidades' three official guide sample reactivos (`docs/guiaoficial.pdf` page 33), only the first (función de la lengua, a Kant maxim in imperative form classified as instrumental rather than informativa) falls inside this unit's scope, calibrating `4.2.1`; the other two (valores → `4.3.2`, bioética → `4.5.1`) belong to future units and are noted here so those units' own calibration audits don't need to re-derive this from the pdf. `hu-funciones-de-la-lengua-01`'s `## Comprobación` documents the calibration boundary — a formally or philosophically worded imperative is still función instrumental, because the function depends on grammatical form and communicative intent, not on the topic's abstractness — and its bank includes an equivalent-shape original reactivo without copying the guide's wording. `content/curriculum/humanidades.ts`'s `hu-4-2` unit and topic titles, previously an unregistered lowercase stub left over from the multi-unit foundation task, were capitalized to conventional spanish directly in this task, applying the same durable finding already established for cultura digital's `cd-2-2`/`cd-2-3`/`cd-2-4`. the other durable cultura digital finding (avoid a predictable index-0 correct-answer pattern) was also applied: each lesson's opening diagnostic+simulator question has its correct option away from index 0 (varied across 1 and 2). `src/features/curriculum/available-units.ts` registers `humanidades`/`hu-4-2-elementos-para-el-pensamiento-y-la-argumentacion` as the registry's 16th entry, right after `hu-4-1`; `question-bank.test.ts` gained a dedicated describe block (matching the precedent set for every area's calibration unit), since this is humanidades' calibration unit, plus the existing `hu-4-1` block was narrowed from asserting the whole bank's length/page to its own 20-question subset, since the bank now spans two units on two different guide pages (14 and 15). registering a second humanidades unit surfaced the same cross-cutting assumptions as every other area's calibration unit, all fixed in the same turn: humanidades' topic count went from 4 to 8, so `/diagnostico/humanidades` is now 8 questions (was 4) and the cross-area `/simulacro` coverage resolver grew from 75 to 79 questions, shifting every question after humanidades' block in the flat coverage sequence (the hardcoded e2e `simulator_coverage_correct_options` fixture and every `pregunta X de 75`/`74 de 75` assertion were updated to 79/78); `/ruta/humanidades` now lists two ready units instead of one (no hardcoded count needed updating there, since that page derives its unit count dynamically). test fixtures that had used `hu-4-2-elementos-para-el-pensamiento-y-la-argumentacion`/`hu-funciones-de-la-lengua-01` ids as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) were repointed to the still-unregistered `hu-4-3-construccion-de-la-persona-para-la-convivencia`/its first lesson id. format check, lint, typecheck, the full unit suite (270 tests, up from 255), `pnpm build` (route list unchanged) and the full 56-test chromium e2e suite all pass; `git diff --check` is clean. next in the queue is `hu-4-3-construccion-de-la-persona-para-la-convivencia`, which is not a calibration unit and should apply this unit's approved findings directly.

23. **`hu-4-3-construccion-de-la-persona-para-la-convivencia` complete — humanidades' third unit, prompt 5 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it confirmed item 22's calibration held (typecheck clean, full unit suite green before starting) and applied its approved findings directly. its three topics (`4.3.1` postulados de las teorías éticas, `4.3.2` los valores y su definición, `4.3.3` tipos de normas) and three single-lesson topics — `hu-teorias-eticas-01`, `hu-valores-para-la-convivencia-01`, `hu-tipos-de-normas-01` — match the source map exactly; unlike `hu-4-2`'s linear prerequisite chain, all three topics have no lesson prerequisites (`ninguno` in the source map), matching `hu-4-1`'s non-foundation-lesson independence rather than `hu-4-2`'s shape. `content/questions/humanidades.ts` gained 15 new records (five per lesson, ids `hu-pte-*`/`hu-vpc-*`/`hu-tdn-*`), for 55 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 15 with its `4.3.x` code, at least one relation question per lesson, and every topic has diagnostic and simulator coverage. of humanidades' three official guide sample reactivos (`docs/guiaoficial.pdf` page 33), the second (relación de elementos matching libertad/justicia/solidaridad/respeto to their definitions) falls inside this unit's scope, calibrating `4.3.2` exactly as item 22 flagged in advance; `hu-valores-para-la-convivencia-01`'s `## Comprobación` documents this and its bank includes an equivalent relación de elementos reactivo over the same four values, with definitions written in original wording rather than the guide's own phrasing. the third sample (bioética) still belongs to a future unit (`4.5.1`), confirmed unchanged. both durable findings from item 19/22 were applied: `content/curriculum/humanidades.ts`'s `hu-4-3` unit and topic titles, previously an unregistered lowercase stub, were capitalized to conventional spanish directly in this task; each lesson's opening diagnostic+simulator question has its correct option away from index 0 (varied across index 1 and index 2). `src/features/curriculum/available-units.ts` registers `humanidades`/`hu-4-3-construccion-de-la-persona-para-la-convivencia` as the registry's 17th entry, right after `hu-4-2`; `question-bank.test.ts` gained a dedicated describe block (same precedent as every calibration/near-calibration humanidades unit so far), covering its own 15-question subset with reserved-id, traceability and relation-question checks. registering a third humanidades unit surfaced the same cross-cutting assumptions as every prior unit in this area, all fixed in the same turn: humanidades' topic count went from 8 to 11, so the cross-area `/simulacro` coverage resolver grew from 79 to 82 questions, shifting every question after humanidades' block in the flat coverage sequence (the hardcoded e2e `simulator_coverage_correct_options` fixture and every `pregunta X de 79`/`78 de 79` assertion were updated to 82/81); `/diagnostico/humanidades` was not asserted with a hardcoded count anywhere, so it needed no fixture change beyond the shared simulator sequence; `/ruta/humanidades` lists three ready units now (no hardcoded count needed updating there, since that page derives its unit count dynamically). test fixtures that had used `hu-4-3-construccion-de-la-persona-para-la-convivencia`/`hu-4-3-1-postulados-de-las-teorias-eticas`/`hu-teorias-eticas-01` ids as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) were repointed to the still-unregistered `hu-4-4-reflexion-politica-y-participacion-ciudadana`/`hu-4-4-1-autonomia-y-heteronomia`/`hu-autonomia-y-heteronomia-01`. new unit tests cover the registry's 17th entry (`available-units.test.ts`, 55-question humanidades bank), `get_available_unit` resolving hu-4-3's three topics (`available-curriculum.test.ts`, including `get_available_units_for_area` now returning three humanidades units), `get_topic_content`/`get_available_topic_content` for a real hu-4-3 topic (`unit-topic-content.test.ts`), and `get_unit_lessons`/`get_available_lesson` for all three no-prerequisite hu-4-3 lessons (`unit-lessons.test.ts`). format check, lint, typecheck, the full unit suite (283 tests, up from 270), `pnpm build` (route list unchanged) and the full 56-test chromium e2e suite all pass (one `--workers=4` run hit a single 30s timeout on the ever-growing base-routes smoke test from dev-server compile contention under parallel load, not a real failure — confirmed by an isolated rerun and a full `--workers=1` run, both green). next in the queue is `hu-4-4-reflexion-politica-y-participacion-ciudadana`.

24. **`hu-4-4-reflexion-politica-y-participacion-ciudadana` complete — humanidades' fourth unit, prompt 6 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it confirmed item 22's calibration held (format check, lint, typecheck and the full unit suite were green before starting) and applied its approved findings directly. its two topics (`4.4.1` autonomía y heteronomía, `4.4.2` discurso político) and two lessons — `hu-autonomia-y-heteronomia-01` (no prerequisite), `hu-discurso-politico-01` (requires `hu-discursos-argumentativos-01`, the last lesson of `hu-4-2`'s linear chain) — match the source map exactly. `content/questions/humanidades.ts` gained 10 new records (five per lesson, ids `hu-ayh-*`/`hu-dp-*`), for 65 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 15 with its `4.4.x` code, one relation question per lesson, and every topic has diagnostic and simulator coverage. of humanidades' three official guide sample reactivos (`docs/guiaoficial.pdf` page 33), none fall inside this unit's scope — the first (función de la lengua) calibrated `4.2.1`, the second (valores) calibrated `4.3.2`, and the third (bioética) belongs to the future `4.5.1` — so no calibration note was needed beyond confirming this against the source map, and both lessons' `## Comprobación` sections say so explicitly. both durable findings from item 19/22 were applied: `content/curriculum/humanidades.ts`'s `hu-4-4` unit and topic titles, previously an unregistered lowercase stub, were capitalized to conventional spanish directly in this task; each lesson's opening diagnostic+simulator question has its correct option away from index 0 (index 1 for `hu-ayh-001`, index 2 for `hu-dp-001`). `src/features/curriculum/available-units.ts` registers `humanidades`/`hu-4-4-reflexion-politica-y-participacion-ciudadana` as the registry's 18th entry, right after `hu-4-3`; `question-bank.test.ts` gained a dedicated describe block (same precedent as every calibration/near-calibration humanidades unit so far), covering its own 10-question subset with reserved-id, traceability and relation-question checks. registering a fourth humanidades unit surfaced the same cross-cutting assumptions as every prior unit in this area, all fixed in the same turn: humanidades' topic count went from 11 to 13, so the cross-area `/simulacro` coverage resolver grew from 82 to 84 questions, shifting every question after humanidades' block in the flat coverage sequence (the hardcoded e2e `simulator_coverage_correct_options` fixture and every `pregunta X de 82`/`81 de 82` assertion were updated to 84/83); `/diagnostico/humanidades` and `/ruta/humanidades`'s unit count are not asserted with hardcoded figures anywhere, so neither needed a fixture change. test fixtures that had used `hu-4-4-reflexion-politica-y-participacion-ciudadana`/`hu-4-4-1-autonomia-y-heteronomia`/`hu-autonomia-y-heteronomia-01` ids as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) were repointed to the still-unregistered `hu-4-5-humanidad-ante-desafios-contemporaneos`/`hu-4-5-1-principios-de-bioetica`/`hu-principios-de-bioetica-01`. new unit tests cover the registry's 18th entry (`available-units.test.ts`, 65-question humanidades bank), `get_available_unit` resolving hu-4-4's two topics (`available-curriculum.test.ts`, including `get_available_units_for_area` now returning four humanidades units), `get_topic_content` for a real hu-4-4 topic (`unit-topic-content.test.ts`), and `get_unit_lessons`/`get_available_lesson` for both hu-4-4 lessons, including the cross-unit prerequisite link to `hu-4-2` (`unit-lessons.test.ts`). the ever-growing `base_routes` smoke test (flagged as environment-only flakiness in item 23) now failed consistently (3/3 full-suite `--workers=4` runs, always passing alone) once four more routes were added on top of the existing ~64; a baseline check on the pre-`hu-4-2` commit confirmed the same test used to run comfortably inside the default 30s timeout, so this is genuine, accumulating drift, not one-off flakiness. fixed with a single `test.setTimeout(60_000)` added to that test — the only production/test-infrastructure change in this task beyond the unit's own content and registration. format check, lint, typecheck, the full unit suite (296 tests, up from 283), `pnpm build` (route list unchanged) and the full 56-test chromium e2e suite all pass, confirmed by a full-suite `--workers=4` run after the timeout fix. next in the queue is `hu-4-5-humanidad-ante-desafios-contemporaneos`.

25. **`hu-4-5-humanidad-ante-desafios-contemporaneos` complete — humanidades' fifth unit, prompt 7 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it confirmed item 22's calibration held (format check, lint, typecheck and the full unit suite were green before starting) and applied its approved findings directly. its five topics (`4.5.1` principios de bioética, `4.5.2` principios éticos aplicados a la sustentabilidad, `4.5.3` perspectiva de género, `4.5.4` reconocimiento de la alteridad, `4.5.5` relación entre humanos y otros seres vivos) and five lessons — `hu-principios-de-bioetica-01` (requires `hu-teorias-eticas-01`), `hu-etica-y-sustentabilidad-01` and `hu-humanos-y-otros-seres-vivos-01` (both requiring `hu-principios-de-bioetica-01`), `hu-perspectiva-de-genero-01` and `hu-reconocimiento-de-la-alteridad-01` (no prerequisites) — match the source map exactly. `content/questions/humanidades.ts` gained 25 new records (five per lesson, ids `hu-pb-*`/`hu-es-*`/`hu-pg-*`/`hu-ra-*`/`hu-hosv-*`), for 90 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 15 with its `4.5.x` code, one relation question per lesson, and every topic has diagnostic and simulator coverage. of humanidades' three official guide sample reactivos, the third (bioética, `docs/guiaoficial.pdf` page 34: a company donating profits to a government food program for people in street situations, illustrating beneficencia) falls inside this unit's scope, calibrating `4.5.1` exactly as items 22–23 flagged in advance; `hu-principios-de-bioetica-01`'s `## Comprobación` documents this and its bank (`hu-pb-004`) includes an equivalent reactivo distinguishing beneficencia from justicia and no maleficencia, with an original scenario rather than the guide's own donation example. both durable findings from item 19/22 were applied: `content/curriculum/humanidades.ts`'s `hu-4-5` unit and topic titles, previously an unregistered lowercase stub, were capitalized to conventional spanish directly in this task; each lesson's opening diagnostic+simulator question has its correct option away from index 0 (index 1 for `hu-pb-001`/`hu-pg-001`/`hu-hosv-001`, index 2 for `hu-es-001`/`hu-ra-001`). `src/features/curriculum/available-units.ts` registers `humanidades`/`hu-4-5-humanidad-ante-desafios-contemporaneos` as the registry's 19th entry, right after `hu-4-4`; `question-bank.test.ts` gained a dedicated describe block (same precedent as every calibration/near-calibration humanidades unit so far), covering its own 25-question subset with reserved-id, traceability, relation-question and opening-index-variety checks. registering a fifth humanidades unit surfaced the same cross-cutting assumptions as every prior unit in this area, all fixed in the same turn: humanidades' topic count went from 13 to 18, so the cross-area `/simulacro` coverage resolver grew from 84 to 89 questions, shifting every question after humanidades' block in the flat coverage sequence (the hardcoded e2e `simulator_coverage_correct_options` fixture and every `pregunta X de 84`/`83 de 84` assertion were updated to 89/88); `/diagnostico/humanidades` and `/ruta/humanidades`'s unit count are not asserted with hardcoded figures anywhere, so neither needed a fixture change. test fixtures that had used `hu-4-5-humanidad-ante-desafios-contemporaneos`/`hu-4-5-1-principios-de-bioetica`/`hu-principios-de-bioetica-01` ids as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) were repointed to the still-unregistered `hu-4-6-reflexiones-sobre-el-arte-y-la-sensibilidad`/`hu-4-6-1-categorias-esteticas`/`hu-categorias-esteticas-01`. new unit tests cover the registry's 19th entry (`available-units.test.ts`, 90-question humanidades bank), `get_available_unit` resolving hu-4-5's five topics (`available-curriculum.test.ts`, including `get_available_units_for_area` now returning five humanidades units), `get_topic_content` for a real hu-4-5 topic (`unit-topic-content.test.ts`), and `get_unit_lessons`/`get_available_lesson` for all five hu-4-5 lessons, including both cross-lesson prerequisite links to `hu-principios-de-bioetica-01` (`unit-lessons.test.ts`). format check, lint, typecheck and the full unit suite (310 tests, up from 296) pass; `pnpm build` (route list unchanged) also passes. this completes humanidades' fifth of six units — one remains (`hu-4-6-reflexiones-sobre-el-arte-y-la-sensibilidad`, the area-closing unit, prompt 8 of 22).

26. **`hu-4-6-reflexiones-sobre-el-arte-y-la-sensibilidad` complete — humanidades' sixth and final unit, prompt 8 of 22 in the expansion queue, completing the area.** its two topics (`4.6.1` categorías estéticas, `4.6.2` definición de hermenéutica) and two single-lesson topics — `hu-categorias-esteticas-01`, `hu-hermeneutica-01`, neither with a lesson prerequisite — match the source map exactly. `content/questions/humanidades.ts` gained 10 new records (five per lesson, ids `hu-ce-*`/`hu-he-*`), for 100 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 15 with its `4.6.x` code, one relation question per lesson, and every topic has diagnostic and simulator coverage. checked directly against `docs/guiaoficial.pdf` pages 33–35: none of humanidades' three official sample reactivos (función de la lengua → `4.2.1`, valores → `4.3.2`, bioética → `4.5.1`) apply to this unit, confirmed exhaustive since all three were already used; page 34's bibliography does list a source titled "los criterios o categorías estéticas" for `4.6.1`, but that is a bibliographic reference, not a sample reactivo, so no calibration note was needed beyond recording this in both lessons' `## Comprobación`. both durable findings from item 19/22 were applied: `content/curriculum/humanidades.ts`'s `hu-4-6` unit and topic titles, previously an unregistered lowercase stub, were capitalized to conventional spanish directly in this task; the two lessons' opening diagnostic+simulator questions have their correct option away from index 0, varied (index 1 for `hu-ce-001`, index 2 for `hu-he-001`). `src/features/curriculum/available-units.ts` registers `humanidades`/`hu-4-6-reflexiones-sobre-el-arte-y-la-sensibilidad` as the registry's 20th entry, right after `hu-4-5`; `question-bank.test.ts` gained a dedicated describe block (same precedent as every other humanidades unit so far, unlike cultura digital's later-units-skip-a-block precedent), covering its own 10-question subset with reserved-id, traceability, relation-question and opening-index-variety checks — also covered generically by `registered-units.test.ts`. registering humanidades' sixth and closing unit surfaced the same cross-cutting assumptions as every prior unit in this area, all fixed in the same turn: humanidades' topic count went from 18 to 20, so the cross-area `/simulacro` coverage resolver grew from 89 to 91 questions, shifting every question after humanidades' block in the flat coverage sequence (the hardcoded e2e `simulator_coverage_correct_options` fixture and every `pregunta X de 89`/`88 de 89` assertion were updated to 91/90); `/diagnostico/humanidades` and `/ruta/humanidades`'s unit count are not asserted with hardcoded figures anywhere, so neither needed a fixture change. since humanidades is now fully registered (all 6 of its source-map units), the fixtures in `available-curriculum.test.ts` (including removing the now-obsolete "no resuelve una unidad de humanidades todavía sin contenido listo" test, following the precedent set when cultura digital completed), `unit-lessons.test.ts` and `unit-topic-content.test.ts` that had used `hu-4-6-reflexiones-sobre-el-arte-y-la-sensibilidad`/`hu-4-6-1-categorias-esteticas`/`hu-categorias-esteticas-01` ids as examples of *unregistered* content were repointed to ciencias sociales' still-unregistered `cs-7-2-perspectivas-politicas`/`cs-7-2-1-teorias-sobre-el-origen-del-estado`/`cs-teorias-sobre-el-origen-del-estado-01`. new unit tests cover the registry's 20th entry (`available-units.test.ts`, 100-question humanidades bank), `get_available_unit` resolving hu-4-6's two topics and `get_available_units_for_area` now returning six humanidades units (`available-curriculum.test.ts`), `get_topic_content` for a real hu-4-6 topic (`unit-topic-content.test.ts`), and `get_unit_lessons` for both hu-4-6 lessons with no prerequisites (`unit-lessons.test.ts`). per the completed-area gate, one focused e2e test was added: completing `hu-categorias-esteticas-01` and practicing `hu-4-6-1-categorias-esteticas` (five questions) reaches `dominado` and survives reload, `hu-4-6-2-definicion-de-hermeneutica` is confirmed `disponible` from the start (no internal prerequisite between the two topics, unlike `cd-2-4`'s chain), and pensamiento matemático is confirmed unaffected. format check, lint, typecheck, the full unit suite (323 tests, up from 310), `pnpm build` (route list unchanged) and the full 57-test chromium e2e suite all pass; `git diff --check` is clean. humanidades is now the third fully-authored area (after pensamiento matemático and cultura digital). next in the queue is `cs-7-2-perspectivas-politicas`, ciencias sociales' calibration unit — not started, per this task's explicit stop-before-ciencias-sociales instruction.

29. **`ch-3-2-mexico-durante-el-expansionismo-capitalista` complete — conciencia histórica's calibration unit, prompt 11 of 22 in the expansion queue.** its ten topics (`3.2.1` causas internas y externas de la independencia through `3.2.10` impacto de la revolución mexicana) and eleven lessons — `ch-causas-de-la-independencia-01`, `ch-proyectos-de-emancipacion-01`, `ch-liberalismo-mexicano-01`/`ch-instituciones-y-leyes-del-liberalismo-02` (the area's second split topic, `3.2.3`, mirroring `3.1.2`'s general-characteristics-then-derived-institutions pattern with exact attribution per law), `ch-intervenciones-extranjeras-del-siglo-xix-01`, `ch-movimientos-sociales-del-siglo-xix-01`, `ch-enajenacion-de-bienes-comunales-01`, `ch-caracteristicas-del-porfiriato-01`, `ch-oposicion-al-porfiriato-01`, `ch-facciones-de-la-revolucion-mexicana-01`, `ch-derechos-originados-en-la-revolucion-01` — match the source map exactly, preserving chronology and causality across the unit (independence causes → emancipation projects → liberal reform and its institutions → foreign interventions → European social-movement context → land dispossession → porfiriato → opposition → revolutionary factions → constitutional rights) even though the guide's own topic order groups the 1838 and 1846–48 foreign interventions (`3.2.4`) after the 1850s liberal reform (`3.2.3`); both lessons and questions state the real dates so the historical sequence stays correct despite the topic numbering. `content/questions/conciencia-historica.ts` gained 55 new records (five per lesson, ids `ch-cdi-*`/`ch-pde-*`/`ch-lbm-*`/`ch-ilb-*`/`ch-iex-*`/`ch-msx-*`/`ch-ebc-*`/`ch-cdp-*`/`ch-oap-*`/`ch-frm-*`/`ch-dor-*`), for 85 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 13 or 14 with its `3.2.x` code, and every topic has diagnostic and simulator coverage. this is conciencia histórica's calibration unit (its second unit but first to require the full first-unit editorial gate, since `ch-3-1` predates the multi-unit expansion and was never itself calibrated under this contract), so the durable findings already established for every other area's calibration unit were applied from the start: `content/curriculum/conciencia-historica.ts`'s `ch-3-2` unit and topic titles, previously an unregistered lowercase stub, were capitalized to conventional spanish directly in this task; each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating index 1/index 2 lesson by lesson. rereading `docs/guiaoficial.pdf` pages 31–32 directly (not just the single sample recorded when `ch-3-1-4` was registered) surfaced two more official conciencia histórica sample reactivos that had gone unused: page 31's second sample (identify the emancipation project matching "división de poderes, soberanía emanada del pueblo, anulación de la esclavitud", correct answer Sentimientos de la Nación, with Ejército Trigarante and independencia de Texas as distractors) calibrates `3.2.2`, and page 32's sample (identify the Revolution faction that took Guerrero and Morelos, fought for campesino/obrero justicia social, and produced the Plan de Ayala, correct answer zapatista) calibrates `3.2.9` — both documented in their lessons' `## Comprobación` with equivalent-but-not-copied reactivos (`ch-pde-001`, `ch-frm-003`). the zapatista sample required one extra resolution step: `3.2.9`'s official orientación names only villista/convencionista/carrancista/constitucionalista, not "zapatista" as its own label, so the lesson explicitly establishes that carrancista and constitucionalista are two names for the same Carranza-led faction, and that the zapatista faction is classified under the convencionista label (the villista–zapatista coalition formed at the Convención de Aguascalientes) — `ch-frm-003`'s correct option is literally `"convencionista"`, verified by a dedicated question-bank test. no current or contested political facts were introduced: the porfiriato, magonismo/PLM and revolutionary-faction lessons stick to settled early-20th-century history with no anachronistic framing, checked by a dedicated question-bank test scoped to this unit's own records. `src/features/curriculum/available-units.ts` registers `conciencia-historica`/`ch-3-2-mexico-durante-el-expansionismo-capitalista` as the registry's 12th entry, right after `ch-3-1` — a **mid-registry insertion**, not an append, since conciencia histórica sits before humanidades, ciencias naturales, lengua y comunicación and ciencias sociales in the registry's area order; this shifts every one of those later areas' positions in the flat cross-area `/simulacro` sequence. `question-bank.test.ts`'s single `ch-3-1`-only describe block (a pre-multi-unit holdover that asserted the whole bank's length and content) was narrowed to its own 30-question subset, matching the pattern already applied to `cs-7-1`, and a new dedicated `ch-3-2` describe block was added, including the split-topic question count check, the zapatista/convencionista calibration check and the no-current-political-facts check. registering conciencia histórica's second unit surfaced the same cross-cutting assumption as every other area's calibration/closing unit, but with the added mid-registry-insertion complication: conciencia histórica's topic count went from 5 to 15, so the cross-area `/simulacro` coverage resolver grew from 107 to 117 questions, and because conciencia histórica precedes humanidades/ciencias naturales/lengua y comunicación/ciencias sociales in the registry, all of those areas' answers in the flat sequence shift by 10 positions — the hardcoded e2e `simulator_coverage_correct_options` fixture had its 10 new answers **inserted** right after `ch-3-1`'s five (not appended at the end), and every `pregunta X de 107`/`106 de 107` assertion was updated to 117/116; the one other hardcoded array index in the file (`servicios_digitales_index = 32`, inside cultura digital's earlier block) was confirmed unaffected since it sits before the insertion point. test fixtures that had used `ch-3-2-mexico-durante-el-expansionismo-capitalista`/its topics/lessons as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` — including a second, previously-missed fixture in its `get_available_lesson` describe block — and `unit-topic-content.test.ts`) were repointed to the still-unregistered `ch-3-3-realidad-actual-en-perspectiva-historica`/its topics/lessons; a new resolved-unit test, a new `get_unit_questions` test, a new eleven-lesson `get_unit_lessons` test and a new real-topic-content test (covering the `3.2.3` split topic) were added in the same places. **playwright was not run this turn**, per the validation cadence set on 2026-09-24 (see item 28): only the focused registered-content integrity tests, the conciencia histórica question-bank tests, typecheck and `git diff --check` were required per unit, though format check, lint, the full unit suite and `pnpm build` were also run here since this unit changed the registry's ordering in a way worth confirming beyond the minimum (format check, lint, typecheck, the full unit suite — 375 tests, up from 340, including one incidental fix to `area-diagnostic.test.ts`'s hardcoded conciencia histórica diagnostic length, 5→15 — `pnpm build` with an unchanged route list, and `git diff --check` all pass); the e2e spec's fixtures were kept current for the eventual single full run at the global audit, but playwright itself was not executed. conciencia histórica is not yet fully authored — `ch-3-3-realidad-actual-en-perspectiva-historica` (its area-closing unit, six topics, requiring the full completed-area gate) remains. next in the queue is `ch-3-3-realidad-actual-en-perspectiva-historica`.

30. **`ch-3-3-realidad-actual-en-perspectiva-historica` complete — conciencia histórica's third and final unit, prompt 12 of 22 in the expansion queue, completing the area.** not a calibration unit, so no new editorial gate; it applied item 29's approved conciencia histórica calibration directly. its six topics (`3.3.1` factores de consolidación del presidencialismo, `3.3.2` participación de México en eventos históricos internacionales del siglo XX, `3.3.3` causas del neoliberalismo en México, `3.3.4` globalización en la actualidad, `3.3.5` causas de la alternancia política, `3.3.6` impacto social de los medios de comunicación en la historia) and six single-lesson topics — `ch-consolidacion-del-presidencialismo-01` (requires `ch-derechos-originados-en-la-revolucion-01`, the last lesson of `ch-3-2`), `ch-mexico-en-eventos-internacionales-01` and `ch-globalizacion-en-la-vida-cotidiana-01` and `ch-impacto-social-de-los-medios-01` (no prerequisite), `ch-causas-del-neoliberalismo-01` and `ch-causas-de-la-alternancia-politica-01` (both requiring `ch-consolidacion-del-presidencialismo-01`) — match the source map exactly. `content/questions/conciencia-historica.ts` gained 30 new records (five per lesson, ids `ch-cpr-*`/`ch-mei-*`/`ch-cnl-*`/`ch-glo-*`/`ch-cap-*`/`ch-ism-*`), for 115 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 14 with its `3.3.x` code, and every topic has diagnostic and simulator coverage. curriculum titles for `ch-3-3` were capitalized to conventional spanish from the start, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, continuing the alternation begun in `ch-3-2` (index 2 for `ch-cpr-001`/`ch-cnl-001`/`ch-cap-001`, index 1 for `ch-mei-001`/`ch-glo-001`/`ch-ism-001`). checked directly against `docs/guiaoficial.pdf` pages 31–32: none of conciencia histórica's three official sample reactivos apply to this unit — all three were already used (`ch-3-1-4`, `ch-3-2-2`, `ch-3-2-9`) — confirmed exhaustive and recorded in every lesson's `## Comprobación`; a dedicated question-bank test asserts all three calibrated records (`ch-oph-001`, `ch-pde-001`, `ch-frm-003`) carry their calibration note, closing the loop on the area's sample-reactivo tracking. this unit covers politically-adjacent 20th-century Mexican history (presidencialismo posrevolucionario, the 1982 debt crisis and neoliberal shift, the 1968 student movement, 1977–1996 electoral reforms, the 1988 election, the 1994 EZLN uprising/Colosio assassination, and the 2000 alternancia) with the same restraint already established for `cs-7-3`: every event is stated as settled, dated, textbook-level history with no partisan framing or reference to any government past 2000; a dedicated question-bank test checks for contested/current-year terms in this unit's own records. conciencia histórica now has 21 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 123 questions (was 117) — this is again a **mid-registry insertion** (conciencia histórica precedes humanidades, ciencias naturales, lengua y comunicación and ciencias sociales in `available_units`), so the e2e `simulator_coverage_correct_options` fixture had its six new answers inserted right after `ch-3-2`'s ten (not appended), and every `pregunta X de 117`/`116 de 117` assertion was updated to 123/122. since conciencia histórica is now fully registered (all three of its source-map units), the old "no resuelve una unidad de conciencia histórica todavía sin contenido listo" test in `available-curriculum.test.ts` was replaced with a real resolved-unit test for `ch-3-3`, and every fixture across `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts` that had used `ch-3-3-realidad-actual-en-perspectiva-historica`/its topics/lessons as examples of *unregistered* content were repointed to ciencias naturales' still-unregistered `cn-5-2-conservacion-de-la-energia-y-sus-interacciones` (and, for one fixture kept distinct to avoid duplication, lengua y comunicación's still-unregistered `lc-6-2-recursos-del-analisis-literario`) — both read directly from their own source maps to get real topic/lesson ids rather than guessed placeholders. `question-bank.test.ts` gained a dedicated `ch-3-3` describe block (reserved-id, traceability, relation/ordering, no-contested-facts and sample-reactivo-tracking checks). format check, lint, typecheck, the full unit suite (391 tests, up from 375, including one incidental fix to `area-diagnostic.test.ts`'s hardcoded conciencia histórica diagnostic length, 15→21) and `pnpm build` (route list unchanged) all pass; `git diff --check` is clean. per the validation cadence set in item 28, playwright was not run this turn — no new e2e test was added either, matching the precedent set by `cs-7-3` (the cadence's completed-area gate no longer requires a new e2e test, only format/lint/typecheck/unit-suite/build) — but the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. conciencia histórica is now the fifth fully-authored area (after pensamiento matemático, cultura digital, humanidades and ciencias sociales). next in the queue is `cn-5-2-conservacion-de-la-energia-y-sus-interacciones`, ciencias naturales' calibration unit — not started, per this task's explicit stop-before-ciencias-naturales instruction.

31. **`cn-5-2-conservacion-de-la-energia-y-sus-interacciones` complete — ciencias naturales' calibration unit, prompt 13 of 22 in the expansion queue.** its five topics (`5.2.1` características de la luz visible, `5.2.2` calor específico, `5.2.3` características de los tipos de energía, `5.2.4` energía cinética y potencial, `5.2.5` leyes de la termodinámica) and five single-lesson topics — `cn-luz-visible-01` (no prerequisite), `cn-calor-especifico-01` (requires `cn-conversion-de-temperatura-01`, from `cn-5-1`), `cn-tipos-de-energia-01` (no prerequisite), `cn-energia-cinetica-y-potencial-01` (requires `cn-tipos-de-energia-01`), `cn-leyes-de-la-termodinamica-01` (requires `cn-calor-especifico-01`) — match the source map exactly. `content/questions/ciencias-naturales-experimentales-y-tecnologia.ts` gained 25 new records (five per lesson, ids `cn-lv-*`/`cn-ce-*`/`cn-te-*`/`cn-ecp-*`/`cn-lt-*`), for 50 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 16 with its `5.2.x` code, and every topic has diagnostic and simulator coverage. this is ciencias naturales' calibration unit, so the durable findings already established for every other area's calibration unit were applied from the start: `content/curriculum/ciencias-naturales-experimentales-y-tecnologia.ts`'s `cn-5-2` unit and topic titles, previously an unregistered lowercase stub, were capitalized to conventional spanish directly in this task; each lesson's opening diagnostic+simulator question has its correct option away from index 0 (index 1 for `cn-lv-001`/`cn-te-001`/`cn-lt-001`, index 2 for `cn-ce-001`/`cn-ecp-001`). per this task's own explicit added instruction, every formula, unit, sign convention and numerical answer was independently verified rather than trusted from the draft: `Q = m × c × ΔT` (`cn-ce-001`–`005`, including the Fahrenheit-to-Celsius conversion step in `cn-ce-004`/`005`), `Ec = (1/2) × m × v²` and `Ep = m × g × h` with `g ≈ 9.8 m/s²` (`cn-ecp-001`–`005`), and the four laws of thermodynamics (`cn-lt-001`–`005`) all check out arithmetically and conceptually. of ciencias naturales' three official guide sample reactivos (`docs/guiaoficial.pdf` pages 35–36), the first (NaCl ionic bond) had already calibrated `cn-5-1-1` in the original mvp content, and the third (respiración celular, glucosa → piruvato) belongs to a future unit (likely `cn-5-3` or `cn-5-6`), not yet in scope; only the second (página 35, identify which example is the ley cero de la termodinámica — ice blocks in mutual contact as the correct answer, a running car engine and a cold aluminum bar submerged in hot water as distractors) falls inside this unit's scope, calibrating `5.2.5` — `cn-leyes-de-la-termodinamica-01`'s `## Comprobación` documents this and its bank (`cn-lt-001`) evaluates the same equilibrio-térmico-vs-segunda-ley distinction with an original two-cups-of-coffee scenario instead of copying the guide's ice-blocks wording. `src/features/curriculum/available-units.ts` registers `ciencias-naturales-experimentales-y-tecnologia`/`cn-5-2-conservacion-de-la-energia-y-sus-interacciones` as the registry's 21st entry, right after `cn-5-1` — another **mid-registry insertion** (ciencias naturales precedes lengua y comunicación and ciencias sociales in the registry's area order), so this shifts both of those later areas' positions in the flat cross-area `/simulacro` sequence. `question-bank.test.ts`'s single `cn-5-1`-only describe block (a pre-multi-unit holdover that asserted the whole bank's length and "no extra records" against the full array) was narrowed to its own 25-question subset and its page-15-traceability check scoped to only its own ids, matching the pattern already applied to `cs-7-1`/`ch-3-1`; a new dedicated `cn-5-2` describe block was added, including the ley-cero calibration check and the opening-index-variety check. registering ciencias naturales' second unit surfaced the same cross-cutting assumption as every other area's calibration unit, with the added mid-registry-insertion complication: ciencias naturales' topic count went from 5 to 10, so the cross-area `/simulacro` coverage resolver grew from 123 to 128 questions, and because ciencias naturales precedes lengua y comunicación and ciencias sociales in the registry, both of those areas' answers in the flat sequence shift by 5 positions — the hardcoded e2e `simulator_coverage_correct_options` fixture had its 5 new answers **inserted** right after `cn-5-1`'s five (not appended at the end), and every `pregunta X de 123`/`122 de 123` assertion was updated to 128/127; the one other hardcoded array index in the file (`servicios_digitales_index = 32`, inside cultura digital's earlier block) was confirmed unaffected since it sits well before the insertion point. test fixtures that had used `cn-5-2-conservacion-de-la-energia-y-sus-interacciones`/its topics/`cn-luz-visible-01` as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) were repointed to ciencias naturales' own still-unregistered `cn-5-3-ecosistemas-interacciones-energia-y-dinamica`/its topics/`cn-fotosintesis-01`; a new resolved-unit test, a new `get_unit_questions` test, a new five-lesson `get_unit_lessons` test (with the `cn-5-1`-dependent prerequisite chain) and a new real-topic-content test were added in the same places, plus a new "resuelve las dos unidades listas de ciencias naturales, en orden de registro" test. one self-caught authoring slip during the mechanical gate: the first draft of the new `cn-5-2` lesson-prerequisite test in `unit-lessons.test.ts` checked three `cn-5-1` lesson ids against the `cn-5-2`-only `lessons` array returned by this test's own `get_unit_lessons` call, so every one of those lookups resolved to `undefined` — fixed by checking the actual remaining `cn-5-2` lessons (`cn-tipos-de-energia-01`'s empty prerequisites and `cn-energia-cinetica-y-potencial-01`'s real dependency on it) instead. per the validation cadence set on 2026-09-24 (item 28): only the focused registered-content integrity tests, the ciencias naturales question-bank tests, typecheck and `git diff --check` were strictly required, since this is not an area completion; the full unit suite (408 tests, up from 391) and `pnpm build` were also run as an extra sanity check given the registry reordering, and both pass. `git diff --check` is clean. playwright was not run this turn; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. ciencias naturales is not yet fully authored — five more units (`cn-5-3` through `cn-5-7`) remain before its area-closing gate. next in the queue is `cn-5-3-ecosistemas-interacciones-energia-y-dinamica`, applying this unit's approved calibration directly.

32. **`cn-5-3-ecosistemas-interacciones-energia-y-dinamica` complete — ciencias naturales' third unit, prompt 14 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it applied item 31's approved ciencias naturales calibration directly. its seven topics (`5.3.1` productos y reactivos en la fotosíntesis through `5.3.7` consecuencias del desequilibrio ecológico) and seven single-lesson topics — `cn-fotosintesis-01` and `cn-biomas-01` and `cn-ciclos-biogeoquimicos-01` and `cn-servicios-ambientales-01` (no prerequisite), `cn-redes-troficas-01` (requires `cn-fotosintesis-01`), `cn-productividad-en-ecosistemas-01` (requires `cn-redes-troficas-01`), `cn-desequilibrio-ecologico-01` (requires `cn-servicios-ambientales-01`) — match the source map exactly. `content/questions/ciencias-naturales-experimentales-y-tecnologia.ts` gained 35 new records (five per lesson, ids `cn-fts-*`/`cn-bio-*`/`cn-rtr-*`/`cn-cbg-*`/`cn-pec-*`/`cn-sam-*`/`cn-deq-*`), for 85 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 16 with its `5.3.x` code, and every topic has diagnostic and simulator coverage — including one ordering question (`cn-rtr-005`, sequencing trophic levels from producer to tertiary consumer) and multiple relation questions. curriculum titles for `cn-5-3` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `cn-fts-001`/`cn-rtr-001`/`cn-pec-001`/`cn-deq-001`, index 2 for `cn-bio-001`/`cn-cbg-001`/`cn-sam-001`). this unit's biology content (photosynthesis equation, trophic levels, carbon/sulfur biogeochemical cycles, gross/net primary/secondary productivity, the three named types of environmental services, and ecological-imbalance consequences) was written from standard, uncontested textbook biology, cross-checked against the source map's own orientación wording rather than any external source; the productivity-calculation questions (`cn-pec-001`/`cn-pec-003`) were verified arithmetically (`PPN = PPB − respiración`: `800 − 200 = 600` and `1,200 − 450 = 750`). ciencias naturales now has 17 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 135 questions (was 128) — another **mid-registry insertion** (ciencias naturales precedes lengua y comunicación and ciencias sociales in the registry), so the e2e `simulator_coverage_correct_options` fixture had its seven new answers inserted right after `cn-5-2`'s five (not appended), and every `pregunta X de 128`/`127 de 128` assertion was updated to 135/134; `servicios_digitales_index = 32` was confirmed unaffected, since it sits well before the insertion point. every fixture that used `cn-5-3`/its topics/`cn-fotosintesis-01` as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) was repointed to ciencias naturales' own still-unregistered `cn-5-4-reacciones-quimicas-y-conservacion-de-la-materia`/its topics/`cn-masa-molar-01`; a new resolved-unit test, a new `get_unit_questions` test (35 questions), a new seven-lesson `get_unit_lessons` test (with the internal fotosíntesis→redes tróficas→productividad and servicios ambientales→desequilibrio chains) and a new real-topic-content test were added in the same places, plus updating the "two ready units" test to three. `question-bank.test.ts` gained a dedicated `cn-5-3` describe block (reserved-id, traceability, relation/ordering and opening-index-variety checks); the `cn-5-2` block's now-stale "fifty records total" assertion (correct only while `cn-5-2` was the area's last unit) was removed, since the new `cn-5-3` block's own "eighty-five records total" test now covers that. per the validation cadence set in item 28, only the focused registered-content integrity tests (238 tests across the six affected files), the ciencias naturales question-bank tests (133 tests, part of that total), typecheck and `git diff --check` were run, since this is not an area completion; all pass. playwright was not run this turn; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. ciencias naturales is not yet fully authored — four more units (`cn-5-4` through `cn-5-7`) remain before its area-closing gate. next in the queue is `cn-5-4-reacciones-quimicas-y-conservacion-de-la-materia`.

33. **`cn-5-4-reacciones-quimicas-y-conservacion-de-la-materia` complete — ciencias naturales' fourth unit, prompt 15 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it applied item 31's approved ciencias naturales calibration directly. its three topics (`5.4.1` masa molar de los compuestos químicos, `5.4.2` tipos de reacciones químicas, `5.4.3` tipos de reacciones nucleares) and three single-lesson topics — `cn-masa-molar-01` (requires `cn-tipos-de-enlaces-01`, from `cn-5-1`), `cn-reacciones-quimicas-01` (requires `cn-conservacion-de-la-materia-01`, from `cn-5-1`), `cn-reacciones-nucleares-01` (no prerequisite) — match the source map exactly. `content/questions/ciencias-naturales-experimentales-y-tecnologia.ts` gained 15 new records (five per lesson, ids `cn-mm-*`/`cn-trq-*`/`cn-rnu-*`), for 100 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 16 with its `5.4.x` code, and every topic has diagnostic and simulator coverage, including one ordering question (`cn-mm-005`, sequencing the molar-mass calculation for `CaCO₃`) and two relation questions (`cn-trq-005`, `cn-rnu-005`). curriculum titles for `cn-5-4` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `cn-mm-001`/`cn-rnu-001`, index 2 for `cn-trq-001`). the molar-mass calculations (`cn-mm-001`–`003`: H₂O = 18 g/mol, CO₂ = 44 g/mol, NaCl = 58.5 g/mol) were verified arithmetically against standard atomic masses (H=1, C=12, O=16, Na=23, Cl=35.5); the reaction-type content (síntesis/descomposición/reducción, fisión/fusión) is standard uncontested chemistry, matching the source map's own orientación wording. of ciencias naturales' three official sample reactivos (`docs/guiaoficial.pdf` pages 35–36), none apply to this unit: the first (NaCl ionic bond) already calibrates `cn-5-1-1`, the second (ley cero) already calibrates `cn-5-2-5`, and the third (respiración celular, glucosa→piruvato) belongs to the still-unregistered `cn-5-6-3-etapas-de-la-respiracion-celular` — confirmed and recorded in all three lessons' `## Comprobación`. ciencias naturales now has 20 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 138 questions (was 135) — another **mid-registry insertion**, so the e2e `simulator_coverage_correct_options` fixture had its three new answers inserted right after `cn-5-3`'s seven (not appended), and every `pregunta X de 135`/`134 de 135` assertion was updated to 138/137; `servicios_digitales_index = 32` confirmed unaffected. every fixture that used `cn-5-4`/its topics/`cn-masa-molar-01` as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) was repointed to ciencias naturales' own still-unregistered `cn-5-5-energia-en-los-procesos-de-la-vida-diaria`/its topics/`cn-choques-elasticos-e-inelasticos-01`; a new resolved-unit test, a new `get_unit_questions` test (15 questions), a new three-lesson `get_unit_lessons` test and a new real-topic-content test were added in the same places, plus updating the "three ready units" test to four. `question-bank.test.ts` gained a dedicated `cn-5-4` describe block (reserved-id, traceability, relation, ordering, calculation and opening-index-variety checks); the `cn-5-3` block's now-stale "eighty-five records total" assertion (correct only while `cn-5-3` was the area's last unit) was removed, since the new `cn-5-4` block's own "one hundred records total" test now covers that. per the validation cadence set in item 28, only the focused registered-content integrity tests (251 tests across the six affected files), the ciencias naturales question-bank tests (142 tests, part of that total), typecheck and `git diff --check` were run, since this is not an area completion; all pass. playwright was not run this turn; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. ciencias naturales is not yet fully authored — three more units (`cn-5-5` through `cn-5-7`) remain before its area-closing gate. next in the queue is `cn-5-5-energia-en-los-procesos-de-la-vida-diaria`.

34. **`cn-5-5-energia-en-los-procesos-de-la-vida-diaria` complete — ciencias naturales' fifth unit, prompt 16 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it applied item 31's approved ciencias naturales calibration directly. its four topics (`5.5.1` tipos de choques, `5.5.2` momento lineal, `5.5.3` ondas electromagnéticas en dispositivos cotidianos, `5.5.4` caída libre) and four single-lesson topics — `cn-choques-elasticos-e-inelasticos-01` and `cn-momento-lineal-01` and `cn-caida-libre-01` (no prerequisite), `cn-ondas-electromagneticas-01` (requiring `cn-luz-visible-01`, a cross-unit dependency on `cn-5-2`) — match the source map exactly. `content/questions/ciencias-naturales-experimentales-y-tecnologia.ts` gained 20 new records (five per lesson, ids `cn-choq-*`/`cn-mli-*`/`cn-oem-*`/`cn-cli-*`), for 120 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 16 with its `5.5.x` code, and every topic has diagnostic and simulator coverage, including one ordering question per calculation lesson (`cn-mli-005`, `cn-cli-005`) and one relation question (`cn-choq-005`, `cn-oem-005`). curriculum titles for `cn-5-5` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `cn-choq-001`/`cn-oem-001`, index 2 for `cn-mli-001`/`cn-cli-001`). the physics content (elastic/inelastic/perfectly-inelastic collisions, `p = m × v` with conservation of linear momentum, electromagnetic-wave types by everyday device, and free fall's `v = g × t`/`h = (1/2) × g × t²` with `g ≈ 9.8 m/s²`) is standard uncontested textbook physics; all momentum and free-fall calculations (`cn-mli-001`–`003`, `cn-cli-001`/`003`) were verified arithmetically. none of ciencias naturales' three official sample reactivos apply to this unit, confirmed and recorded in all four lessons' `## Comprobación` (the third, respiración celular, still belongs to the still-unregistered `cn-5-6-3-etapas-de-la-respiracion-celular`). ciencias naturales now has 24 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 142 questions (was 138) — another **mid-registry insertion**, so the e2e `simulator_coverage_correct_options` fixture had its four new answers inserted right after `cn-5-4`'s three (not appended), and every `pregunta X de 138`/`137 de 138` assertion was updated to 142/141; `servicios_digitales_index = 32` confirmed unaffected. every fixture that used `cn-5-5`/its topics/lessons as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) was repointed to ciencias naturales' own still-unregistered `cn-5-6-organismos-estructura-y-procesos`/its topics/`cn-organelos-celulares-01`, and — since `cn-5-6` only has three topics against the four fixtures needed in one describe block — the third block's three-fixture set was repointed to `cn-5-7`'s first three topics instead, both read directly from the source map; a new resolved-unit test, a new `get_unit_questions` test (20 questions), a new four-lesson `get_unit_lessons` test (including the cross-unit `cn-luz-visible-01` dependency) and a new real-topic-content test were added in the same places, plus updating the "four ready units" test to five. `question-bank.test.ts` gained a dedicated `cn-5-5` describe block (reserved-id, traceability, relation, ordering, calculation and opening-index-variety checks); the `cn-5-4` block's now-stale "one hundred records total" assertion was removed, since the new `cn-5-5` block's own "one hundred twenty records total" test now covers that. per the validation cadence set in item 28, only the focused registered-content integrity tests (264 tests across the six affected files), the ciencias naturales question-bank tests (151 tests, part of that total), typecheck and `git diff --check` were run, since this is not an area completion; all pass. playwright was not run this turn; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. ciencias naturales is not yet fully authored — two more units (`cn-5-6`, `cn-5-7`) remain before its area-closing gate. next in the queue is `cn-5-6-organismos-estructura-y-procesos`.

35. **`cn-5-6-organismos-estructura-y-procesos` complete — ciencias naturales' sixth unit, prompt 17 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it applied item 31's approved ciencias naturales calibration directly. its three topics (`5.6.1` organelos celulares, `5.6.2` niveles de organización biológica, `5.6.3` etapas de la respiración celular) and three single-lesson topics — `cn-organelos-celulares-01` and `cn-niveles-de-organizacion-biologica-01` (no prerequisite), `cn-respiracion-celular-01` (requiring `cn-organelos-celulares-01`) — match the source map exactly. `content/questions/ciencias-naturales-experimentales-y-tecnologia.ts` gained 15 new records (five per lesson, ids `cn-org-*`/`cn-nob-*`/`cn-rec-*`), for 135 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 16 with its `5.6.x` code, and every topic has diagnostic and simulator coverage, including one ordering question per non-calibration lesson (`cn-nob-005`, `cn-rec-005`) and one relation question (`cn-org-005`). curriculum titles for `cn-5-6` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `cn-org-001`/`cn-rec-001`, index 2 for `cn-nob-001`). **this unit resolves the third and final official ciencias naturales sample reactivo**, tracked since `cn-5-2` (item 31): `docs/guiaoficial.pdf` page 36 asks which molecule breaks down in the first stage of cellular respiration (glucose → 2 pyruvate + energy). the pdf was re-read directly this turn to confirm the exact wording and correct answer (`A) glucosa - piruvato`) before writing the lesson. `cn-respiracion-celular-01`'s `## Comprobación` documents this and its bank includes an equivalent completamiento reactivo on a different stage — the piruvato→acetil-CoA transition, with `CO₂` release — testing the same substance-identification skill without copying the guide's stage or wording (`cn-rec-003`, carrying a `calibración de profundidad: página 36` source-reference note, verified by a dedicated question-bank test). the biology content (cell organelles by eukaryotic/prokaryotic presence, the célula→tejido→órgano→organismo→población→comunidad→ecosistema hierarchy, and the four-stage glucólisis→acetil-CoA→ciclo de Krebs→cadena de transporte de electrones pathway) is standard uncontested textbook biology, cross-checked against the source map's own orientación wording. ciencias naturales now has 27 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 145 questions (was 142) — another **mid-registry insertion**, so the e2e `simulator_coverage_correct_options` fixture had its three new answers inserted right after `cn-5-5`'s four (not appended), and every `pregunta X de 142`/`141 de 142` assertion was updated to 145/144; `servicios_digitales_index = 32` confirmed unaffected. every fixture that used `cn-5-6`/its topics/lessons as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) was repointed to ciencias naturales' own still-unregistered `cn-5-7-herencia-y-evolucion-biologica`/its topics/lessons — including the earlier item-34 fixture split (a four-topic block that had borrowed one slot from `cn-5-7` since `cn-5-6` only had three topics), now cleanly repointed entirely within `cn-5-7` to avoid any duplication across describe blocks; a new resolved-unit test, a new `get_unit_questions` test (15 questions), a new three-lesson `get_unit_lessons` test and a new real-topic-content test were added in the same places, plus updating the "five ready units" test to six. `question-bank.test.ts` gained a dedicated `cn-5-6` describe block (reserved-id, traceability, relation, ordering, the acetil-CoA calibration check and opening-index-variety checks); the `cn-5-5` block's now-stale "one hundred twenty records total" assertion was removed, since the new `cn-5-6` block's own "one hundred thirty-five records total" test now covers that. per the validation cadence set in item 28, only the focused registered-content integrity tests (277 tests across the six affected files), the ciencias naturales question-bank tests (160 tests, part of that total), typecheck and `git diff --check` were run, since this is not an area completion; all pass. playwright was not run this turn; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. ciencias naturales is not yet fully authored — one unit (`cn-5-7-herencia-y-evolucion-biologica`) remains before its area-closing gate, which requires the full completed-area gate (format check, lint, the full unit suite, `pnpm build`, and per the `cs-7-3`/`ch-3-3`/`hu-4-6` precedent, no new e2e test unless production behavior changed). next in the queue is `cn-5-7-herencia-y-evolucion-biologica`.

36. **`cn-5-7-herencia-y-evolucion-biologica` complete — ciencias naturales' seventh and final unit, prompt 18 of 22 in the expansion queue, closing the area.** not a calibration unit, so no new editorial gate; it applied item 31's approved ciencias naturales calibration directly. its five topics (`5.7.1` reproducción sexual y asexual through `5.7.5` consecuencias de la evolución) and five single-lesson topics — `cn-reproduccion-sexual-y-asexual-01` and `cn-tipos-de-cromosomas-01` and `cn-teorias-evolutivas-01` (no prerequisite), `cn-cuadros-de-punnett-01` (requiring `cn-tipos-de-cromosomas-01`), `cn-consecuencias-de-la-evolucion-01` (requiring `cn-teorias-evolutivas-01`) — match the source map exactly, confirmed directly against `docs/guiaoficial.pdf` page 17. `content/questions/ciencias-naturales-experimentales-y-tecnologia.ts` gained 25 new records (five per lesson, ids `cn-rsa-*`/`cn-toc-*`/`cn-cdp-*`/`cn-tev-*`/`cn-cev-*`), for 160 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 17 with its `5.7.x` code, and every topic has diagnostic and simulator coverage, including one ordering-style relación de elementos question per lesson except `cuadros-de-punnett` (`cn-rsa-004`, `cn-toc-004`, `cn-cev-004`, all written in the bank's established `1c, 2a, 3b` numeric-relation format, not the roman-numeral format first drafted and corrected before any test ran). curriculum titles for `cn-5-7` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task, including "Cuadros de Punnett" (proper noun), and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `cn-rsa-001`/`cn-cdp-001`, index 2 for `cn-toc-001`/`cn-tev-001`/`cn-cev-001`). all three of ciencias naturales' official sample reactivos were already used by `cn-5-1-1` (item 31), `cn-5-2-5` (item 31) and `cn-5-6-3` (item 35), confirmed exhaustive against pages 35–36, so no calibration note was needed for this unit — every lesson's `## Comprobación` records this explicitly. the genetics/evolution content (sexual vs. asexual reproduction and its five mechanisms, chromosome classification by centromere position and by function, Punnett-square genotypic/phenotypic ratios for monohybrid crosses, natural selection vs. Lamarckian transformism, and divergent evolution/morphological adaptation/molecular convergence) is standard uncontested textbook biology. ciencias naturales now has 32 topics total (matching the source map's own validation count) and is fully authored — the sixth area to reach this state (after pensamiento matemático, cultura digital, humanidades, ciencias sociales and conciencia histórica). the cross-area `/simulacro` coverage grows by one question per topic, not per question record: it is now 150 questions (was 145) — another **mid-registry insertion**, so the e2e `simulator_coverage_correct_options` fixture had its five new answers inserted right after `cn-5-6`'s three (not appended), and every `pregunta X de 145`/`144 de 145` assertion was updated to 150/149. since ciencias naturales is now fully registered, the "resuelve la unidad... cn-5-7" tests replaced the old not-yet-registered checks, and every fixture that used `cn-5-7`/its topics/lessons as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) was repointed to lengua y comunicación's still-unregistered `lc-6-2-recursos-del-analisis-literario`/its real planned topics and lesson id (`lc-tema-central-del-texto-narrativo-01`, read from its own source map); a new resolved-unit test, a new `get_unit_questions` test (25 questions), a new five-lesson `get_unit_lessons` test (checking both internal prerequisite chains) and a new real-topic-content test were added in the same places, plus updating the "six ready units" test to seven. `question-bank.test.ts` gained a dedicated `cn-5-7` describe block (reserved-id, traceability to page 17, relation, ordering and opening-index-variety checks, no calibration-specific test since none applies to this unit); the `cn-5-6` block's now-stale "one hundred thirty-five records total" assertion was removed, since the new `cn-5-7` block's own "one hundred sixty records total" test now covers that. per the completed-area gate, format check, lint, typecheck, the full unit suite (490 tests, up from 391) and `pnpm build` (route list unchanged) all pass; `git diff --check` is clean. playwright was not run per the `cs-7-3`/`ch-3-3`/`hu-4-6` precedent (no new e2e test added at this area's close either, since no shared production code/routes/progress/diagnostic/simulator/learning-ui behavior changed) — the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. ciencias naturales is now the sixth fully-authored area. the only area left in the 22-unit queue is lengua y comunicación (`lc-6-2` through `lc-6-5`, prompts 19–22); next in the queue is `lc-6-2-recursos-del-analisis-literario`, lengua y comunicación's calibration unit.

37. **`lc-6-2-recursos-del-analisis-literario` complete — lengua y comunicación's second unit, prompt 19 of 22 in the expansion queue, its calibration unit.** its six topics (`6.2.2` tema central del texto literario narrativo through `6.2.7` tipos de tiempo narrativo — the guide's own numbering skips `6.2.1`, preserved as a documented source gap, not filled in) and six single-lesson topics — `lc-tema-central-del-texto-narrativo-01`, `lc-personajes-del-texto-narrativo-01`, `lc-narrador-del-texto-narrativo-01`, `lc-ambito-de-la-narracion-01` and `lc-tiempo-narrativo-01` (no prerequisite), `lc-trama-del-texto-narrativo-01` (requiring `lc-tema-central-del-texto-narrativo-01`) — match the source map exactly. `content/questions/lengua-y-comunicacion.ts` gained 30 new records (five per lesson, ids `lc-tcn-*`/`lc-trn-*`/`lc-pnj-*`/`lc-nar-*`/`lc-amb-*`/`lc-tpn-*`), for 50 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 17 with its `6.2.x` code, and every topic has diagnostic and simulator coverage, including one relación de elementos question per lesson (`lc-tcn-005`, `lc-trn-005`, `lc-pnj-005`, `lc-nar-005`, `lc-amb-005`, `lc-tpn-005`, all in the bank's established `1c, 2a, 3b` numeric format). curriculum titles for `lc-6-2` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task (this is lengua y comunicación's calibration unit, since `lc-6-1` predates the multi-unit contract), and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `lc-tcn-001`/`lc-pnj-001`/`lc-amb-001`, index 2 for `lc-trn-001`/`lc-nar-001`/`lc-tpn-001`). all six lessons teach literary-analysis skills (theme vs. plot, the planteamiento/nudo/desenlace structure, direct vs. indirect characterization, narrator type by grammatical person and knowledge of characters' thoughts, spatial/temporal/social setting, and linear vs. retrospective narrative time) that require a short passage to apply, so most questions carry an original `stimulus` field with an invented narrative excerpt, written from scratch and never copied from any external source. `docs/guiaoficial.pdf` pages 37–38 were re-read directly to confirm lengua y comunicación's three official sample reactivos exhaustively: fuentes de información (página 37), puntuación faltante and orden de un párrafo coherente (página 38) — all three fall inside the still-unregistered `lc-6-3-procesos-de-composicion-de-textos`, none inside `lc-6-2`'s scope, exactly as `lc-jerarquia-en-mapas-conceptuales-01` (from the pre-multi-unit `lc-6-1`) had already documented; a dedicated question-bank test confirms no `lc-6-2` record carries a calibration note. lengua y comunicación now has 10 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 156 questions (was 150) — a **mid-registry insertion**, since lengua y comunicación precedes ciencias sociales in `available_units`, so the e2e `simulator_coverage_correct_options` fixture had its six new answers inserted right after `lc-6-1`'s four (not appended), and every `pregunta X de 150`/`149 de 150` assertion was updated to 156/155. every fixture that used `lc-6-2`/its topics/lessons as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) was repointed to lengua y comunicación's own still-unregistered `lc-6-3-procesos-de-composicion-de-textos`/its topics/lessons; a new resolved-unit test, a new `get_unit_questions` test (30 questions), a new six-lesson `get_unit_lessons` test (checking the one internal prerequisite) and a new real-topic-content test were added in the same places, plus updating the area's "ready units" test from one to two. `question-bank.test.ts` gained a dedicated `lc-6-2` describe block (reserved-id, traceability to page 17, relation, opening-index-variety and no-calibration-applies checks), and the `lc-6-1` block's whole-bank "has exactly twenty records"/"no extra records" assertions were narrowed to their own four-lesson subset, matching the precedent set by every other area's first post-pilot unit. per the validation cadence, only the focused registered-content integrity tests (296 tests across five files), the lengua y comunicación question-bank tests (part of that total), typecheck and `git diff --check` were required, since this is not an area completion; all pass; `git diff --check` is clean. playwright was not run this turn, matching the precedent set by every other area's own calibration unit; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. lengua y comunicación is not yet fully authored — three more units (`lc-6-3` through `lc-6-5`) remain before its area-closing gate. next in the queue is `lc-6-3-procesos-de-composicion-de-textos`, which should apply this unit's approved calibration (no editorial gate needed) but does need to apply the three sample reactivos flagged above.

38. **`lc-6-3-procesos-de-composicion-de-textos` complete — lengua y comunicación's third unit, prompt 20 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it applied item 37's approved calibration directly. its seven topics (`6.3.1` etapas para la composición de un ensayo through `6.3.7` adecuación de un texto) and seven single-lesson topics — `lc-composicion-de-un-ensayo-01`, `lc-tipos-de-fuentes-de-informacion-01`, `lc-reglas-de-acentuacion-01`, `lc-reglas-de-puntuacion-01`, `lc-unidades-sintacticas-01`, `lc-coherencia-textual-01`, `lc-adecuacion-textual-01`, none with a lesson prerequisite — match the source map exactly. `content/questions/lengua-y-comunicacion.ts` gained 35 new records (five per lesson, ids `lc-cde-*`/`lc-tfi-*`/`lc-rac-*`/`lc-rpu-*`/`lc-uds-*`/`lc-cht-*`/`lc-adt-*`), for 85 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, and `source_reference` to guide page 17 (topics `6.3.1`–`6.3.3`) or page 18 (topics `6.3.4`–`6.3.7`) with its own `6.3.x` code, matching the source map's documented page split; a dedicated question-bank test checks each half against its correct page. curriculum titles for `lc-6-3` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `lc-cde-001`/`lc-rac-001`/`lc-uds-001`/`lc-cht-001`/`lc-adt-001`, index 2 for `lc-tfi-001`/`lc-rpu-001`). **this unit resolves all three of lengua y comunicación's official sample reactivos**, tracked since `lc-6-2` (item 37): `docs/guiaoficial.pdf` page 37's fuentes-de-información sample (relating primary/secondary/tertiary sources to three example texts) calibrates `6.3.2` (`lc-tfi-005`); page 38's puntuación-faltante sample calibrates `6.3.4` (`lc-rpu-005`); page 38's párrafo-coherente ordering sample calibrates `6.3.6` (`lc-cht-005`) — all three lessons' `## Comprobación` document this and their bank includes an equivalent reactivo with wholly original texts (a meteorological-records scenario, a gym-coach scenario, a beehive-ordering scenario), never copying the guide's own wording, examples or subject matter; each calibrated record's `source_reference` carries a `calibración de profundidad: página 37`/`38` note, verified exhaustively by a dedicated question-bank test. every lesson has at least one relation-or-ordering question, a genuine content property, not just a passing test: `lc-cde-005` and `lc-cht-005` use ordering (etapas de composición; secuencia de un párrafo), the rest use the bank's established `1c, 2a, 3b` numeric relation format — `lc-rpu-004` was specifically written as a relation question (matching puntuation situations to their signs) so that reglas de puntuación, whose calibrated `-005` is a multi-blank completamiento with no digit-led options, still satisfies that invariant. lengua y comunicación now has 17 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 163 questions (was 156) — another **mid-registry insertion**, since lengua y comunicación precedes ciencias sociales in `available_units`, so the e2e `simulator_coverage_correct_options` fixture had its seven new answers inserted right after `lc-6-2`'s six, and every `pregunta X de 156`/`155 de 156` assertion was updated to 163/162. every fixture that used `lc-6-3`/its topics/lessons as examples of *unregistered* content was repointed to lengua y comunicación's own still-unregistered `lc-6-4-formas-orales-de-la-comunicacion`/its topics/lessons; a new resolved-unit test, a new `get_unit_questions` test (35 questions), a new seven-lesson `get_unit_lessons` test and a new real-topic-content test were added in the same places, plus updating the area's "ready units" test from two to three. `question-bank.test.ts` gained a dedicated `lc-6-3` describe block (reserved-id, split-page traceability, relation-or-ordering-per-lesson, opening-index-variety and the three-sample-calibration checks), replacing the `lc-6-2` block's now-stale "fifty records total, across the area's two units" assertion; the `lc-6-1` block's own two whole-bank checks (its "relation question per lesson" and "guide code on page 17" tests, both previously unscoped) were narrowed to their own four-lesson subset in the same turn, once `lc-6-3`'s page-18 records would otherwise have broken them — the same narrowing precedent applied at every other area's own third-unit turn. per the validation cadence, only the focused registered-content integrity tests (308 tests across five files), the lengua y comunicación question-bank tests (part of that total), typecheck and `git diff --check` were required, since this is not an area completion; all pass; `git diff --check` is clean. playwright was not run this turn; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. lengua y comunicación is not yet fully authored — two more units (`lc-6-4`, `lc-6-5`) remain before its area-closing gate, which requires the full completed-area gate. next in the queue is `lc-6-4-formas-orales-de-la-comunicacion`.

39. **`lc-6-4-formas-orales-de-la-comunicacion` complete — lengua y comunicación's fourth unit, prompt 21 of 22 in the expansion queue.** not a calibration unit, so no new editorial gate; it applied item 37's approved calibration directly. its four topics (`6.4.1` elementos de la exposición oral through `6.4.4` funciones de las formas orales de la comunicación) and four single-lesson topics — `lc-elementos-de-la-exposicion-oral-01` and `lc-caracteristicas-del-dialogo-01` (no prerequisite), `lc-elementos-del-debate-01` (requiring `lc-caracteristicas-del-dialogo-01`), `lc-funciones-del-dialogo-y-debate-01` (requiring `lc-elementos-del-debate-01`) — match the source map exactly. `content/questions/lengua-y-comunicacion.ts` gained 20 new records (five per lesson, ids `lc-eeo-*`/`lc-cdi-*`/`lc-edb-*`/`lc-fdd-*`), for 105 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 18 with its `6.4.x` code, and every topic has diagnostic and simulator coverage, including one relación de elementos question per lesson (`lc-eeo-005`, `lc-cdi-005`, `lc-edb-005`, `lc-fdd-005`, all in the bank's established `1c, 2a, 3b` numeric format). curriculum titles for `lc-6-4` (a lowercase stub left over from the multi-unit foundation task) were capitalized to conventional spanish directly in this task, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `lc-eeo-001`/`lc-edb-001`, index 2 for `lc-cdi-001`/`lc-fdd-001`). `docs/guiaoficial.pdf` pages 37–38 were checked directly again: none of lengua y comunicación's three official sample reactivos apply to this unit — all three were already used by `lc-6-3` (item 38) — confirmed and recorded in all four lessons' `## Comprobación`. the four oral-communication skills taught (exposición oral's five elements — planeación, apoyos gráficos, manejo de voz, manejo del espacio, manejo del tiempo; diálogo's three elements — preguntas detonadoras, intercambio de información, turnos de palabra; debate's three elements — posturas, contraposición de ideas, argumentos; and the diálogo-vs-debate function distinction) are standard uncontested communication-skills content, matching the source map's own orientación wording exactly. lengua y comunicación now has 21 topics total, which changed the cross-area figure previously stated as fixed: the cross-area `/simulacro` coverage is now 167 questions (was 163) — another **mid-registry insertion**, since lengua y comunicación precedes ciencias sociales in `available_units`, so the e2e `simulator_coverage_correct_options` fixture had its four new answers inserted right after `lc-6-3`'s seven (not appended), and every `pregunta X de 163`/`162 de 163` assertion was updated to 167/166. every fixture that used `lc-6-4`/its topics/lessons as examples of *unregistered* content (in `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts`) was repointed to lengua y comunicación's own still-unregistered `lc-6-5-estructura-gramatical-del-ingles`/its topics/lesson ids (`lc-tiempo-presente-en-ingles-01`, `lc-tiempo-pasado-en-ingles-01`), read directly from its source map; a new resolved-unit test, a new `get_unit_questions` test (20 questions), a new four-lesson `get_unit_lessons` test (checking the unit's linear prerequisite chain) and a new real-topic-content test were added in the same places, plus updating the area's "ready units" test from three to four. `question-bank.test.ts` gained a dedicated `lc-6-4` describe block (reserved-id, traceability to page 18, relation, opening-index-variety and no-calibration-applies checks), replacing the `lc-6-3` block's now-stale "eighty-five records total, across the area's three units" assertion. per the validation cadence, only the focused registered-content integrity tests (320 tests across five files, all passing), typecheck and `git diff --check` were required, since this is not an area completion; all pass; `git diff --check` is clean. playwright was not run this turn; the e2e spec's fixtures were kept accurate for the eventual single full run at the global audit. lengua y comunicación is not yet fully authored — one unit (`lc-6-5-estructura-gramatical-del-ingles`, its area-closing english-grammar unit) remains before its area-closing gate, which requires the full completed-area gate (format check, lint, the full unit suite, `pnpm build`, and per the `cs-7-3`/`ch-3-3`/`hu-4-6`/`cn-5-7` precedent, no new e2e test unless production behavior changed). this is also the last remaining unit in the entire 22-unit queue; after it, `docs/tasks/2026-09-23-task_expansion-auditoria-global.md` runs as the final global audit. next in the queue is `lc-6-5-estructura-gramatical-del-ingles`.

40. **`lc-6-5-estructura-gramatical-del-ingles` complete — lengua y comunicación's fifth and final unit, prompt 22 of 22, closing both the area and the entire 22-unit expansion queue.** not a calibration unit, so no new editorial gate; it applied item 37's approved calibration directly. its ten topics (`6.5.1` tiempo presente through `6.5.10` voz pasiva) and ten single-lesson topics — `lc-tiempo-presente-en-ingles-01` and `lc-comparaciones-en-ingles-01` (no prerequisite), `lc-tiempo-pasado-en-ingles-01`/`lc-tiempo-futuro-en-ingles-01`/`lc-presente-perfecto-en-ingles-01`/`lc-preguntas-wh-en-ingles-01`/`lc-verbos-modales-en-ingles-01`/`lc-estructuras-condicionales-en-ingles-01` (requiring `lc-tiempo-presente-en-ingles-01`), `lc-pasado-perfecto-en-ingles-01`/`lc-voz-pasiva-en-ingles-01` (requiring `lc-tiempo-pasado-en-ingles-01`) — match the source map exactly, confirmed directly against `docs/guiaoficial.pdf` page 18. `content/questions/lengua-y-comunicacion.ts` gained 50 new records (five per lesson, ids `lc-tpi-*`/`lc-tpa-*`/`lc-tfu-*`/`lc-ppf-*`/`lc-psp-*`/`lc-pwh-*`/`lc-rdc-*`/`lc-vbm-*`/`lc-cnd-*`/`lc-vzp-*`), for 155 total in that bank; each has three distinct options, an explanation, a concrete `common_error`, `source_reference` to guide page 18 with its `6.5.x` code, and every topic has diagnostic and simulator coverage, including one relación de elementos question per lesson in the bank's established `1c, 2a, 3b` numeric format, matching english example sentences or words to their correct grammatical category. curriculum titles for `lc-6-5` were capitalized to conventional spanish, and each lesson's opening diagnostic+simulator question has its correct option away from index 0, alternating (index 1 for `lc-tpi-001`/`lc-tfu-001`/`lc-psp-001`/`lc-rdc-001`/`lc-cnd-001`, index 2 for `lc-tpa-001`/`lc-ppf-001`/`lc-pwh-001`/`lc-vbm-001`/`lc-vzp-001`). this is the area's one english-language unit — explanations, error notes and objectives stay in spanish, matching every other lesson in the app, while example sentences and question options are in english, since the skill tested is identifying correct english grammatical constructions, matching the guide's own orientación wording ("identificación de la construcción correcta de oraciones... en inglés") almost verbatim; every grammar rule (presente simple vs. continuo, pasado simple vs. continuo, will vs. going to, presente perfecto's no-specific-time-marker rule, pasado perfecto's before/after ordering, wh- question words with correct auxiliary use, comparative/superlative formation including the irregular good→better→best, modal-verb nuances for can/should/must/might, first vs. second conditional, and passive voice formation with `be` + participio pasado) was checked against standard english-grammar rules before writing; no factual grammar errors. checked directly against `docs/guiaoficial.pdf` pages 37–38 again: none of lengua y comunicación's three official sample reactivos apply to this unit — all three were already used by `lc-6-3` (item 38) — confirmed and recorded in all ten lessons' `## Comprobación`. lengua y comunicación now has 31 topics total (matching the source map's own validation count) and is fully authored — the seventh and final area to reach this state. the cross-area `/simulacro` coverage grows by one question per topic, not per question record: it is now 177 questions (was 167) — another **mid-registry insertion**, so the e2e `simulator_coverage_correct_options` fixture had its ten new answers inserted right after `lc-6-4`'s four (not appended), and every `pregunta X de 167`/`166 de 167` assertion was updated to 177/176. **this completes all 34 units across all seven mvp areas**, matching the sum of every area source map's own "unidades" validation count (`6+4+3+6+7+5+3=34`) — every unit is now registered in `available_units`, with no still-unregistered content remaining anywhere in the curriculum. every fixture across `available-curriculum.test.ts`, `unit-lessons.test.ts` and `unit-topic-content.test.ts` that previously pointed at `lc-6-5`/its topics/lessons as examples of *unregistered* content was repointed to synthetic, clearly nonexistent ids (`lc-6-6-unidad-inexistente` and equivalents), since no real still-unregistered unit exists anywhere to point to any longer; the area's own "no resuelve unidad todavía sin contenido listo" test was replaced with a real `lc-6-5` resolution test and a real ten-lesson `get_unit_lessons` test (checking the full prerequisite graph), matching the precedent set at every other area's completion. `question-bank.test.ts` gained a dedicated `lc-6-5` describe block (reserved-id, traceability to page 18 using a code regex that correctly handles the two-digit topic number in `6.5.10`, relation, opening-index-variety and no-calibration-applies checks), replacing the `lc-6-4` block's now-stale "one hundred and five records total" assertion. per the completed-area gate, format check, lint, typecheck, the full unit suite (554 tests, up from 490 recorded at the previous completed-area gate) and `pnpm build` (route list unchanged) all pass; `git diff --check` is clean. playwright was not run, matching the `cs-7-3`/`ch-3-3`/`hu-4-6`/`cn-5-7` precedent, since no shared production code/routes/progress/diagnostic/simulator/learning-ui behavior changed; the e2e spec's fixtures were kept accurate for the single full run that now follows. **all 22 prompts in the 2026-09-23 expansion queue are now complete.** the next and final step is `docs/tasks/2026-09-23-task_expansion-auditoria-global.md`: the complete chromium e2e suite runs once as part of that audit, and per `CLAUDE.md`'s own standing instruction, auth and synchronization work does not begin unless that audit's result is explicitly `ready`.

41. **global content audit complete — `docs/tasks/2026-09-23-task_expansion-auditoria-global.md`, the final content boundary before auth/synchronization.** seven parallel independent reviews (one per area, each re-solving every question in its own bank and re-reading every lesson against `docs/guiaoficial.pdf`) covered all 184 lessons and 920 questions, not a sample. inventory confirmed exactly as expected from production data: 7 areas, 34 units (registered once each, in area-contiguous order matching `available_units`), 177 topics, 184 unique lesson ids, 920 questions; diagnostics pensamiento matemático 30/cultura digital 18/conciencia histórica 21/humanidades 20/ciencias naturales 32/lengua y comunicación 31/ciencias sociales 25; the `/simulacro` coverage resolver validated 177 unique questions, one per topic, no duplicates. the global prerequisite graph (all 184 lessons across all areas) has no missing targets and no cycles. three content defects were found and fixed, each preserving all stable ids: `pm-sel-004` (`content/questions/pensamiento-matematico.ts`) had a relación-de-elementos system of equations that didn't actually solve to its own offered answer — corrected the equation; `ch-pde-001` (`content/questions/conciencia-historica.ts`) reproduced `docs/guiaoficial.pdf`'s own sample-reactivo sentence near-verbatim instead of testing the same fact with original wording — rewritten; `lc-uds-005` (`content/questions/lengua-y-comunicacion.ts`) labeled "predicado" as mutually exclusive from its own complements, contradicting its lesson's explicación — relabeled and its explanation corrected. cultura digital, ciencias naturales and ciencias sociales came back fully clean. two non-defects were flagged for visibility only, not fixed, since neither is a genuine defect: conciencia histórica's `ch-intervenciones-extranjeras-del-siglo-xix-01` lists a chronologically-later prerequisite for a thematic (not strictly chronological) unit grouping, already self-documented in the lesson's own explicación; humanidades' `hu-4-1` lessons all have their opening question's correct answer at index 0, predating the "vary the opening index" finding established starting with `hu-4-2`, matching the same historical-inconsistency precedent already accepted for every other area's pre-calibration content. the manual UI inspection (screenshots across `cn-5-2`'s unit page, a calculation lesson, a long-reading practice page with narrative stimulus, a split-topic practice page, incorrect-answer feedback, the diagnostic selector and an area diagnostic, the simulacro landing, `/progreso`, and a mobile english-grammar lesson, in both light and dark theme) surfaced one real production-code defect beyond content: `src/features/lesson/lesson-view.tsx` rendered a lesson's raw prerequisite id ("Repasa cn-conversion-de-temperatura-01") to learners instead of a human-readable title — every other "repasar" affordance in the app already resolved a title. fixed by making `LessonView` resolve each prerequisite's title via `get_available_lesson` before rendering; this required correcting six `e2e/navigation.spec.ts` assertions that had hardcoded the old buggy raw-id text as their expected value. incorrect-answer feedback was confirmed not to depend on color alone (an `×` icon and explicit "Por reforzar."/"Respuesta correcta:" text accompany the red/green styling). verification: format check, lint and typecheck clean; the full unit suite passes (554 tests); `pnpm build` succeeds with an unchanged route list; the full chromium e2e suite passes (57/57) — one simulator-report test is confirmed flaky under parallel `--workers` load from dev-server compile contention (passes in isolation and in a full `--workers=1` run), the same pre-existing pattern already documented at items 23–24 and 26, not a regression; `git diff --check` is clean. no unresolved item required a stable-id, curriculum-boundary or product-scope decision. full results, the audit-item-by-audit-item accounting and the `ready` decision are in `docs/tasks/2026-09-23-report_expansion-auditoria-global-resultados.md`. per `CLAUDE.md`'s standing instruction, auth and synchronization work may now begin.
